Section container. No cards with shadows: sections are separated by rules (double rule = major section, single = sub-section).
```jsx
<Panel kicker="Presidente · Brasil" title="Resultado parcial" action={<Button size="sm" variant="ghost">Metodologia</Button>}>…</Panel>
```