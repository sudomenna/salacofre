Pointer-following tooltip for UF / município hover on the map. Position with the pointer's offset inside the map container.
```jsx
<HoverCard x={pt.x} y={pt.y} kicker="Unidade federativa" title="Minas Gerais" apurado={71} rows={[{name:"Lula",color:"var(--party-pt)",pct:47.1,proj:48.0},…]} />
```