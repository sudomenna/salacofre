---
title: Riscos e Mitigações
description: Matriz de riscos do projeto com impacto, probabilidade e mitigação por linha
status: stable
source: PRD.md § 22; ADR-0020 (revisão dos riscos TSE em 2026-09-05)
---

# Riscos e Mitigações


## Tarefas do usuário — atualizado em 2026-09-12

Não são risco técnico: são ações que só o dono da conta pode executar, e cada uma **bloqueia**
trabalho que já está pronto do lado do código.

| Tarefa | Prazo | O que destrava | Estado |
|---|---|---|---|
| **Reconectar a integração Git da Vercel** (painel do projeto → Settings → Git) e disparar um deploy real de `main` — **não** um "Redeploy", que reconstrói o commit antigo | **antes de 15/09** | **Tudo.** A produção está em 17/05, 122 commits atrás; nada do pipeline, do modelo ou das telas jamais foi ao ar. Enquanto isso não passar, as variáveis criadas hoje não valem e o simulado não tem o que testar | 🔴 **aberto — maior pendência do projeto** |
| **Abrir chamado no TSE** (`30308800.tse.jus.br`, descrição começando com `Resultados - Divulgação`) | **12/09** | As URLs do simulado. `pnpm tse:watch --once` segue sem sinal de 2026 — `ele-c.json` só lista `ele2024` e nenhum dos 9 leiautes mudou. **Reconferido em 12/09 às 21h39 e 22h48 BRT: idêntico.** ✅ **RESOLVIDO em 17/09, e a causa era nossa**: o 403 vinha de o segmento do caminho ser o **ambiente**, e `oficial` ser o de produção. O endereço certo do simulado é `resultados-sim.tse.jus.br/simulado/simulado2026`. **403 ali nunca significou "ainda não publicado"** — e foi essa leitura errada que fez a vigia não ver o ambiente subir em 14/09, custando os dias 15 e 16 da primeira janela (zero commits em 14, 15 e 16/09). ⚠️ **Sem código de eleição o ciclo LANÇA** — `getCodEleicao` em `lib/tse/targets.ts:332`, throws nas linhas 340 e 349. (Esta linha citava `targets.ts:285` até 2026-09-18; aquela linha é o `buildEA20Url` deprecated. Desde o ADR-0044 as variáveis são `TSE_COD_ELEICAO_FEDERAL` e `TSE_COD_ELEICAO_ESTADUAL`; a `TSE_COD_ELEICAO` sobrevive só como fallback legado e nunca supre de forma cruzada.)** O chamado não é burocracia — sem a resposta, a ingestão não roda em nenhum ambiente. | 🟡 **ABERTO PELO USUÁRIO EM 13/09** — aguardando resposta do TSE. Três perguntas feitas: (1) o `codEleicao` dos simulados de 15–17/09 e 22–24/09; (2) o endereço base correto do ambiente de simulado, já que `resultados-sim.tse.jus.br/oficial/comum/config/ele-c.json` responde **403**; (3) se os códigos de 2026 entram no `ele-c.json` de produção **antes** dos simulados ou só a partir de 03/10, junto com os parâmetros oficiais. ⚠️ **O que fazer enquanto não responde**: `pnpm tse:watch --once` **diariamente**. Rodado em 13/09 às 00h22 BRT: `changed=false`, `ele-c.json` ainda só com `ele2024` e nenhum dos 9 leiautes alterado. ⚠️ **Se não houver resposta até 15/09 de manhã**, o protocolo do simulado **para no Passo 1** — não prosseguir por tentativa e erro: 404 por URL adivinhada bloqueia o IP por 10 minutos e queima uma das quatro janelas de teste. Telefone do canal: (61) 3030-8800. |
| ✅ ~~**Gerar `EDGE_CONFIG_TOKEN`**~~ **FEITO 12/09** em `vercel.com/account/tokens`, **escopo do time** `team_AqxGDYz4Zxs5wUBUDzIpcwBm` (pessoal recebe 403). Colar no `.env.local` junto de `EDGE_CONFIG_ID=ecfg_mcoa3usgvm5dbqb27vae8ptmpdxl`. | antes de 15/09 | **Escopo corrigido em 11/09.** Destrava **uma** coisa: a gravação no Global Config, que nunca rodou de verdade — `writeEdgePayload` (`lib/edge-config/writer.ts:193-211`) é no-op amigável sem credencial e loga `global-config write skipped: missing credentials`. **NÃO** bloqueia mais o Blob, o painel municipal nem o gate de a11y: aquilo era outro defeito (a rota `/api/_internal/edge-write` estava sob *private folder* do App Router e respondia 404 em todo ambiente) e foi corrigido em 11/09, commit `0b858b5`. O Blob usa `BLOB_READ_WRITE_TOKEN`, que já existe, e recebeu escrita real no mesmo dia. Depois: `pnpm edge-config:smoke`. ⚠️ **Atualização de 12/09**: colar este token era o gatilho de um defeito crítico no gravador (primeira linha de *Riscos ativos*) — o Senador sobrescreveria a chave do Presidente. **Corrigido em 12/09, antes de o token existir.** Gerar o token agora é seguro. | ✅ **FEITO em 12/09.** `pnpm edge-config:smoke` passou nos 4 passos obrigatórios. Medições reais: gravação 713 ms, leitura 730 ms, store com **106 B de 1.000.000 B (0,01%)** e 2 itens. ⚠️ **Faltava uma terceira variável, não documentada em lugar nenhum**: `VERCEL_TEAM_ID` (`vercelApiUrl` anexa `?teamId=`; token de time sem ela recebe 403/404). ⚠️ E o token precisa ter escopo de **time**, não de projeto: o primeiro token criado foi limitado ao projeto e lia `/v9/projects` mas recebia **403 em todo recurso de time** (`/v1/edge-config`, `/v1/storage/stores`) e **404 em `/v2/user`` — o upsert voltava `404 Edge Config Item not found`, que parece store inexistente e não é. O `ecfg_mcoa3usgvm5dbqb27vae8ptmpdxl` documentado **está correto**, agora verificado. |


## Riscos ativos

| Risco | Impacto | Probabilidade | Mitigação |
|---|---|---|---|
| 🔴 **TODA a configuração do TSE existe apenas em `Preview` — em produção a ingestão LANÇA ERRO a cada ciclo.** Observado no painel da Vercel em 2026-09-19, coluna *Environment* do projeto `salacofre`: `TSE_BASE_URL`, `TSE_COD_ELEICAO_FEDERAL`, `TSE_COD_ELEICAO_ESTADUAL`, `TSE_MAX_RPS`, `TSE_GRANULARIDADE`, `TSE_ACOMPANHAMENTO`, `TSE_TARGETS_WHITELIST`, `INGEST_WINDOW` e `CRON_ENABLED` estão **todas** marcadas só `Preview`. Nenhuma em `Production`. ⚠️ **Não é degradação silenciosa, é falha dura**: `getCodEleicao` (`lib/tse/targets.ts:332-348`) não tem default e **`throw`** quando nem a env específica nem o legado `TSE_COD_ELEICAO` existem — o próprio docstring (`:325-330`) explica que autorizar um default ali seria o modo de falha "default silencioso em conversor de enum" que esta base já pagou 4 vezes. ⚠️ `TSE_BASE_URL` **tem** default (`https://resultados.tse.jus.br/oficial`, `targets.ts:80`), então produção aponta para o lugar certo e morre no código da eleição — o que torna o sintoma ainda mais estreito e mais difícil de atribuir. | **CRÍTICO, e é o pior modo de falha do produto**: o cron dispara, a ingestão lança, nada é buscado, nada é calculado, nada é publicado — e a tela mostra **"Aguardando o primeiro boletim"**, que é **indistinguível** da tela legítima de antes das 17h. O site fica plausível a noite inteira enquanto não apura nada. Mesma família dos 3 defeitos que esta base já teve (`0b858b5` rota sob *private folder*, `MODEL_SECRET` ausente, gravador de Global Config no-op): o elo final degrada com `warn` e ninguém lê | **Confirmada** — leitura direta do painel em 19/09, não inferência. O que **não** foi verificado: se existe um `TSE_COD_ELEICAO` (legado) em Production fora das linhas visíveis da tela | 🔴 **Aberto, só o dono resolve, prazo 04/10.** ⚠️ **NÃO copiar Preview → Production**: os valores de lá são do **simulado** (`21270`/`21272`, `resultados-sim.tse.jus.br`), e copiá-los faria produção buscar o simulado no dia da eleição — virar para o real exige trocar **endereço e códigos juntos** (memória do projeto, `tse_simulado_parametros`). Sequência correta: (1) manter como está até o simulado de 22–24/09, que **precisa** delas em Preview; (2) **depois** criar as variáveis em `Production` com os valores reais; (3) ⚠️ os códigos reais **ainda não são certeza** — produção publicou `6257`/`6259` em 18/09 e eles **sumiram** em 19/09 (memória `tse_codigos_producao_2026`). **Verificação de pronto**: um POST manual a `/api/ingest` em produção com `x-cron-secret` tem de devolver `ok:true` e gravar `snapshots` — hoje devolveria erro. Ver [runbook § Ferramentas do pipeline TSE](../operations/runbook.md) |
| 🔴 **O domínio `salacofre.com.br` ainda não foi configurado — e é o endereço que declaramos ao TSE.** Medido em 2026-09-19: `curl https://salacofre.com.br` estoura o timeout de 25s, e o DNS resolve para **189.125.92.230**, que não é endereço da Vercel (provável página do registrador). `www` não resolve. `salacofre.vercel.app` responde 200 normalmente — é só o domínio próprio. **Confirmado pelo dono em 19/09: ele ainda não apontou o domínio**, então isto é tarefa pendente, não defeito. | **Médio a alto, e com dois eixos distintos.** (1) **Conformidade**: o [ADR-0020](../architecture/adrs/0020-conformidade-res-23751-2026.md) fixa que o `USER_AGENT` faz *identificação honesta do projeto, **URL pública e contato verificável***, e o valor em produção é `SalaCofre/1.0 (+https://salacofre.com.br; contato: contato@salacofre.com.br)` (`lib/tse/client.ts`, `docs/specs/001-ingestao-tse/design.md:297-298`). Hoje **nenhum dos dois é verificável**: o site não abre, e o e-mail depende do mesmo domínio — sem registro MX, não entrega. Declarar identificação não verificável a uma autoridade eleitoral é o oposto do que o ADR quis resolver ao remover a menção falsa a cadastro. (2) **Público**: é o endereço que o leitor vai digitar em 04/10 | **Confirmada** (DNS + `curl` em 19/09) — e **não é surpresa**: o dono confirmou que ainda não configurou | 🔴 **Aberto, só o dono resolve.** ⚠️ **Não deixar para a véspera**: apontar o domínio na Vercel dispara emissão de certificado TLS e propagação de DNS, que **não são instantâneas** — e o e-mail exige registro **MX** separado, que não vem junto com o apontamento do site. **Prazo sugerido: antes do simulado de 22–24/09**, para que o `USER_AGENT` já esteja verificável na única janela de ensaio com tráfego real contra o TSE. Alternativa, se o domínio não ficar pronto a tempo: trocar o `USER_AGENT` para a URL que **funciona** (`salacofre.vercel.app`) — identificação verificável vale mais que endereço bonito, e o ADR-0020 exige a primeira, não o segundo |
| **O ritmo de escrita da série por candidatura nunca foi medido sob carga.** Desde 2026-09-17 cada ciclo do modelo grava, além do que já gravava, `pct_atual`/`votos_atuais`/`dado_ts` por candidatura (migration 0009, **aplicada em produção**). Com o escopo decidido pelo dono (cargos 1, 3 e 5 — Deputado Federal fora), uma noite de 8h a 60s por ciclo produz **~424 mil linhas**: Presidente nacional+27 UFs ~175 mil, Senador ~153 mil, Governador ~96 mil. Hoje a tabela inteira tem **13.258 linhas** — a noite de 04/10 vai multiplicá-la por **32 em oito horas**, e o banco nunca escreveu nesse ritmo. | **Médio a alto na noite da apuração.** Se o INSERT começar a demorar, ele atrasa o ciclo inteiro: a persistência acontece **dentro** da mesma transação que fecha o cálculo, antes de o payload ser publicado. Um ciclo que estoura o orçamento de 60s atrasa a tela, não só o banco | **Não medida.** O volume acima é aritmética sobre contagens reais do cadastro (13 candidaturas a Presidente, 200 a Governador, 319 a Senador); o comportamento do Neon sob essa escrita, não | **Aberto, prazo 04/10.** Medir antes do simulado de 22-24/09: rodar o ciclo contra um banco descartável com o elenco cheio e cronometrar o INSERT. Se apertar, os caminhos são (a) `execute_values`/COPY em vez de `executemany`, (b) reduzir a cadência de gravação da série sem mexer na do modelo, ou (c) mover a persistência para fora da transação do ciclo. O índice `ix_proj_serie` já existe e **acrescenta custo de escrita** — vale medir com e sem ele. Ver [spec 020](../specs/020-evolucao-da-apuracao/spec.md) e [handoff de 17/09](../_meta/handoff-2026-09-17.md) |
| ✅ **FECHADO em 2026-09-13.** ~~O cargo 6 soma pares de município×zona SEM a trava de sanidade que protege os outros três.~~ **A trava cobre os quatro cargos desde o commit `550fbb2`** (*"feat(model): a trava contra multiplicação de votos passa a cobrir o cargo 6"*, 13/09 04:33 BRT), e ele **está na `main`** — `git merge-base --is-ancestor 550fbb2 main` é verdadeiro, ou seja, não ficou preso num worktree como os dois commits de 13/09 que só chegaram à `main` em 17/09. `check_zona_merge_sanity` é chamada no ramo proporcional em `api/model/project.py:5658`, dentro de `_do_project_proporcional` (`:5573`), no modo **sem merge** (`merged_rows=None`): soma `Σ e.te` dos pares de cada zona e compara com o eleitorado real dela, mesmo limiar `>= 1,8` do ramo majoritário, e sem abortar o ciclo (§ 7). O modo tem docstring própria em `api/model/zona_merge.py:565-574`. Cobertura medida em 19/09 com `.venv-model/bin/python3.14 -m pytest`: `tests/unit/model/test_zona_merge_sanity.py` + `test_zona_merge_real.py` = **20 verdes** (o limiar), mais o **teste de fio** que prova que a guarda é de fato *chamada* no ciclo — `test_deputado_payload.py -k guarda` = **3 verdes** (ramo proporcional, com o par "dispara quando a razão é 2,0" / "fica calada quando os pares trazem fatias") e `test_orchestrator.py -k guarda` = **1 verde** (ramo majoritário). ⚠️ **Esta linha afirmou o contrário por seis dias, até 2026-09-19**, e citava `api/model/project.py:4171`, `:3832` e `api/model/deputado.py:412` — referências que o crescimento dos arquivos já havia invalidado, e que por isso não podiam ser conferidas por quem lesse. A mesma afirmação vencida foi repetida no handoff de 19/09 § 3.1 (corrigido) e na **mensagem do commit `16d4a26`, seção "Dívida registrada"** — essa não pode ser corrigida, porque já foi empurrada; o desmentido vive em [handoff-2026-09-19](../_meta/handoff-2026-09-19.md) § 3.1. **Sobre a origem**: a exposição foi criada pelo ADR-0036 em 13/09 (antes, o cargo 6 vinha em granularidade UF e não havia o que somar), e o ADR **afirmava por escrito que a trava vinha junto** — era falso quando escrito, foi apontado por outra sessão na mesma madrugada, e deixou de ser falso horas depois, com `550fbb2`. | **Era alto na noite da apuração**: se a premissa da fatia por município (Passo 0) fosse falsa, os votos de Deputado multiplicariam por até 8× em ~62% das zonas **em silêncio**, enquanto Presidente, Governador e Senador gritavam — a bancada da Câmara sairia grosseiramente errada sem nenhum alarme. **Hoje o cargo 6 grita junto.** O que resta não é a trava, é o **alcance do grito** e uma condição que o desliga — os dois resíduos na coluna ao lado. | Da multiplicação em si: depende inteiramente da premissa do Passo 0, que segue **não verificada** contra dado real. Do resíduo (1): **certa** — a variável não existe hoje. Do resíduo (2): depende de `fetch_eleitorado` falhar no ciclo, não medida. | `pnpm verify-fatia-premise` (Passo 0) decide a premissa **antes** do simulado e cobre os quatro cargos — segue sendo a mitigação principal. A rede de runtime que faltava **existe desde 13/09**. **Restam dois resíduos, e nenhum deles é a trava:** **(1) O alarme não tem destino.** A trava dispara `_log("error", …)` e `_alert_slack(…)`; `_alert_slack` (`api/model/project.py:5250`) lê `os.environ.get("SLACK_WEBHOOK_URL")` e, ausente a variável, loga `"slack alert skipped — SLACK_WEBHOOK_URL ausente"` e retorna — best-effort que nunca levanta (§ 7). Conferido em 19/09: a variável **não está** entre as 27 chaves do `.env.local`, e continua na lista de pendências do dono. Na prática, em produção uma multiplicação de votos de Deputado vira **uma linha de log na Vercel que ninguém lê na noite da apuração**. Isto é exatamente o objetivo declarado da sprint ativa **S08 — Enxergar**, e está travado no dono. **(2) A guarda vive dentro de um `if eleitorado:`** (`api/model/project.py:5657`). Logo acima, `fetch_eleitorado` tem um `except` que degrada para `eleitorado = {}` com um `_log("warn", "fetch_eleitorado falhou no ciclo proporcional", …)` — degradação **deliberada**, constituição § 7: perder o eleitorado piora o `pct_apurado_total`, derrubar o ciclo apagaria a bancada inteira da tela. Consequência não registrada em lugar nenhum até agora: **uma falha na consulta ao eleitorado desliga a trava em silêncio.** Registrado aqui como observação, **não como defeito a consertar já** — o trade-off é legítimo. O que muda é a leitura do log: ausência de alarme de sanidade passa a ter duas causas possíveis (nada errado, ou eleitorado indisponível), e o `warn` do `fetch_eleitorado` é o que as distingue. |
| **CAUSA RAIZ do congelamento da produção: uma *rolling release* parada há 4 meses esperando aprovação manual** — descoberta em 12/09 ao tentar promover o deploy novo, que respondeu *"Promotion has been queued and will begin when the active rolling release completes successfully"*. Estado via `/v1/projects/<id>/rolling-release`: `state: ACTIVE`, `activeStage` índice **0**, `currentCanaryPercentage: 10`, `advancementType: manual-approval`, `startedAt` **17/05/2026 06:58**. Os estágios configurados são 10% → 50% → 100%, e **os dois primeiros exigem aprovação humana** que nunca veio. Enquanto isso, `salacofre.vercel.app` serve o deploy de **17/05 06:00** para 90% do tráfego e o canário de 06:58 para 10%, e todo deploy novo entra em **fila** sem assumir o endereço (`aliasAssigned: false`). | ✅ **FECHADO em 13/09.** A configuração foi **desligada** — `vercel rolling-release configure --disable` —, verificado pela API: `rollingRelease: null` no projeto e nenhuma release ativa. O próximo deploy vai direto a 100%. Histórico do destrave, para quem repetir: Sequência que funcionou, pela CLI com `--token`/`--scope`: `vercel rolling-release abort --dpl=<deploy ESTÁVEL>` (⚠️ o `--dpl` do abort é o destino do rollback, **não** o canário — a ajuda do comando diz o contrário e passar o canário devolve 400), depois `vercel promote` — que **iniciou uma rolling release nova, de novo nos 10%** —, e por fim `vercel rolling-release complete --dpl=<CANÁRIO>`. Só então os aliases moveram. `salacofre.vercel.app` serve o build de 12/09 desde 13/09. 🔴 **Falta desligar** em Settings → Rolling Releases: enquanto estiver ligada, todo deploy para nos 10% esperando dois cliques. | **Crítico, e muito pior no dia D** — com esta configuração, um conserto publicado na noite da apuração chegaria a **10% dos leitores** até alguém clicar em aprovar, duas vezes. É a configuração exatamente errada para um produto cujo valor inteiro está concentrado em 6 horas | **Confirmada** (API da Vercel, 12/09) | ⚠️ **Corrige uma atribuição errada minha, feita horas antes**: eu havia registrado o `git_info_fail` como a causa do congelamento. Ele é **real e separado** — impede o build automático a partir do GitHub —, mas **não** é o que segura o endereço. Mesmo com o Git funcionando, todo deploy teria ficado na fila desde maio. **Caminho**: `vercel rolling-release abort` (cancela o canário de maio), depois **desligar ou reconfigurar** as rolling releases — senão o deploy seguinte para no mesmo 10% —, e então `vercel promote <deploy>`. A CLI aceita `--token` e `--scope`, o que contorna a conta errada. **Decisão do usuário**: manter publicação gradual com aprovação manual é defensável fora da janela eleitoral e indefensável dentro dela. |
| **O site em produção está 122 commits e 4 meses atrasado — a Vercel não constrói desde 17/05/2026** — medido em 12/09 pela API da Vercel: o último deploy `READY` de produção é de **17/05/2026 15:11**, commit `3e8068f` (*"docs(s02): close S02"*). `git rev-list --count 3e8068f..HEAD` = **122**. O redeploy manual de 12/09 23:40 **falhou** com `errorCode: git_info_fail`. **Nada construído desde a S02 jamais foi para o ar**: nem o pipeline de ingestão, nem o modelo, nem as 4 rotas do redesign, nem Senador, nem Deputado. | ✅ **RESOLVIDO em 13/09** — `salacofre.vercel.app` serve o código de 12/09, verificado nas 5 rotas (títulos mudaram de "AtlasMenna" para "SalaCofre") e a tela de Deputado inspecionada no ar: degrada corretamente sem dado, diz **"não são uma projeção"** e declara a granularidade por UF como escolha nossa. O deploy saiu pela CLI com `--token`, contornando a integração Git — que **continua quebrada** e segue pendente. | **Crítico, e reenquadra vários outros achados** — os defeitos do gravador, da rota sob *private folder* e do `MODEL_SECRET` nunca puderam se manifestar porque o código nunca rodou em produção. Também significa que **nenhuma medição de produção feita até hoje vale**: elas mediram o build de maio | **Confirmada** (API da Vercel, `/v6/deployments` e `/v13/deployments/<id>`, 12/09) | ⚠️ **ATUALIZADO em 12/09, mais tarde: esta NÃO é a causa do congelamento.** A causa é a *rolling release* parada (primeira linha desta tabela). O `git_info_fail` é um segundo problema, real e independente: impede o build automático a partir do GitHub, mas não segura o endereço. Os dois precisam ser resolvidos. **Aberto — só o usuário resolve, antes de 15/09.** O que já foi **descartado** como causa: a ligação está íntegra nos metadados — `link.type` github, `org/repo` = `sudomenna/salacofre`, `productionBranch` = `main`, e o `repoId 1241324365` **bate exatamente** com `gh api repos/sudomenna/salacofre --jq .id`. O repositório é **público** e tem `pushed_at` de hoje. Não há deploy hook configurado (0). ✅ **CAUSA CONFIRMADA em 13/09 por observação direta**: `github.com/settings/installations` mostra **"No installed GitHub Apps"** — o GitHub App da Vercel **não está instalado** na conta `sudomenna`. A autorização OAuth continua lá (`github.com/settings/apps/authorizations` lista Vercel, "last used within the last 2 months"), e é por isso que o painel abre e o login funciona: **identificar o usuário e ler o repositório são permissões diferentes**, e só a segunda sumiu. Conserto: instalar em `github.com/apps/vercel/installations/new`, concedendo acesso a `salacofre`. ✅ **Instalado pelo usuário em 13/09.** ⚠️ **Instalar não bastou**: a Vercel passou a enxergar o repositório (`/v1/integrations/git-namespaces` devolve a instalação com `isAccessRestricted: false`, e `search-repo` acha `salacofre` com o id certo), mas um push **não disparou build** por 18 minutos. `vercel git connect` respondeu *"already connected"* e não fez nada — o registro existia, o webhook não. Só **`vercel git disconnect` seguido de `connect`** refez a ligação. Prova em andamento — um push tem de disparar build automático, verificável em `/v6/deployments` com `source: git` (os deploys de 12/09 saíram com `source: cli`). ⚠️ Provável mesma origem da rolling release travada — as duas param em **17/05**. ~~Hipótese restante, a verificar no painel: a instalação do GitHub App da Vercel perdeu acesso ao repositório.~~ Caminho: painel do projeto → Settings → Git → reconectar/reinstalar a integração, e então **um deploy de verdade a partir de `main`**, não um redeploy (redeploy reconstrói o commit do deploy anterior — foi por isso que ele tentou o de maio). ⚠️ **Esperar 122 commits construírem de primeira é otimista**: o build de produção nunca foi exercitado com este código. Reservar tempo para consertar o que aparecer. |
| **A CLI da Vercel nesta máquina está logada na conta errada** — `vercel teams ls` em 12/09 devolve só `bruna-pugas-projects` ("Bruna Puga's projects"); o `salacofre` é de `sudomenna`. Por isso `vercel env ls` responde *"Could not retrieve Project Settings"*. | **Baixo** hoje, **médio no dia D**: qualquer comando da Vercel rodado às pressas age na conta errada ou não acha o projeto — e um `vercel env pull` sobrescreveria o `.env.local` com o de outro projeto | **Confirmada** (12/09) | Espelha o mesmo problema já conhecido no `gh`, que exige `gh auth switch --user sudomenna` antes de todo push e devolver depois. Acertar antes de 15/09. Enquanto isso, o caminho que **funciona** é a API com o `EDGE_CONFIG_TOKEN` do `.env.local` (escopo de time), que foi como toda a inspeção de 12/09 foi feita. ✅ **Deploy hook criado e TESTADO em 13/09** (`emergencia-dia-d`, id `HkVUtPrjIs`, branch `main`): o disparo produziu deploy `READY` com `deployHookId` correto e `target: production`. URL é segredo, vive em `.env.local` como `VERCEL_DEPLOY_HOOK_URL`. Procedimento no topo de [runbook.md](../operations/runbook.md). |
| **O projeto na Vercel não tem `MODEL_SECRET` — o modelo NUNCA é disparado em produção** — listadas as 26 variáveis do projeto `prj_ZEDsW5C8cOr7WredhPZUsx2tm2R3` em 12/09 com token de time: há `CRON_SECRET`, `EDGE_CONFIG`, `BLOB_READ_WRITE_TOKEN` e as 18 do Neon; **não há** `MODEL_SECRET`, `EDGE_CONFIG_TOKEN`, `VERCEL_TEAM_ID` nem nenhuma `TSE_*`. A cadeia quebra no segundo elo: o cron chama `/api/ingest/<cargo>` com `CRON_SECRET` (existe) ✅ → `lib/tse/ingest-handler.ts:762` tenta disparar o modelo, não acha `MODEL_SECRET`, loga `model-trigger skipped` e **segue como se estivesse tudo bem** ❌. Nada é calculado, nada é publicado. E ainda que fosse, `post_edge_write` pularia pelo mesmo motivo, e a rota `/api/internal/edge-write` aborta sem o segredo. | **Crítico** — em produção a ingestão grava snapshots e o site nunca mostra nada novo, **sem nenhum erro visível**: só linhas de `warn` que ninguém lê | **Confirmada** (listagem real da API da Vercel em 12/09, não inferência) | **Parcialmente fechado em 12/09.** `MODEL_SECRET`, `EDGE_CONFIG_TOKEN` e `VERCEL_TEAM_ID` foram criadas pelo usuário em **production + preview** e conferidas pela API (`/v9/projects/<id>/env`). ⚠️ **`VERCEL_TEAM_ID` FOI aceita** — a ressalva de que a Vercel recusaria o prefixo `VERCEL_` não se confirmou. ⚠️ **Mas nada disso está no ar**: o deploy está travado desde maio (primeira linha desta tabela), então as variáveis novas só passam a valer quando um build passar. **Falta ainda** acrescentar: `MODEL_SECRET` (o mesmo valor do `.env.local`), `EDGE_CONFIG_TOKEN` e as `TSE_*` que o simulado exigir (`TSE_BASE_URL`, `TSE_COD_ELEICAO`, `INGEST_WINDOW`, `TSE_MAX_RPS`, `TSE_GRANULARIDADE`). ⚠️ `VERCEL_TEAM_ID` pode ser **recusada pelo painel** (a Vercel reserva o prefixo `VERCEL_`) — se for, é renomear a variável no código, ~10 min. **Mesma família do defeito de 11/09** (rota sob *private folder* devolvendo 404 em silêncio, commit `0b858b5`): o último elo do pipeline que nunca foi exercitado porque o caminho inteiro degrada com `warn` em vez de falhar. |
| **Cor-base de partido abaixo do piso de contraste para elemento gráfico (WCAG 1.4.11)** — medido em 12/09, em modo **claro**, do token base contra `--surface-page` / `--surface-card`: **PSOL 2,08 / 2,21** e **NOVO 2,72 / 2,89**, contra piso de **3:1**. PSDB passa raspando (3,02). Em modo escuro os 11 tokens medidos passam com folga (pior caso 3,09). O filete divisor do `<VoteBar>` não salva: 2,21 contra PSOL. | **Médio** — o texto (sigla) sempre acompanha a cor, então WCAG 1.4.1 (não depender só de cor) está satisfeito; o que falha é 1.4.11, no segmento da barra, que é o canal visual primário (`showLabels={false}`) | **Confirmada** — recalculei de forma independente com a fórmula de luminância da WCAG 2.1 e bati com o gate ao centésimo | **Aberto.** O defeito é do **gerador da paleta** (`scripts/gen-party-scale.ts` → `app/tokens-party.css`, GERADO — corrigir à mão é desfeito na próxima geração), não das telas de Deputado, e alcança todo consumidor de `colorForParty` como preenchimento de área: `_NationalChoroplethMapImpl.tsx:293` (mapa nacional), `StateResultSheet.tsx:106`, `StrongholdsPanel.tsx` — rotas **já no ar**. ⚠️ **O que mudou em 12/09 não é a causa, é a exposição**: em Presidente/Governador um estado só fica amarelo-PSOL se o PSOL liderar ali, o que praticamente não ocorre; na tela de Deputado o PSOL **lidera a federação PSOL-Rede**, então aquela barra aparece em toda apuração, sempre. **Por que escapou dos gates existentes**: `party-chip-contrast.test.ts` e `party-text-contrast.test.ts` medem só os pares `-chip`/`-ink` e `-text`/papel; `party-separation.test.ts` mede só ΔE entre partidos. **Ninguém mede o token base como preenchimento contra o fundo da página.** E o `axe-core` não acusa (16/16 combinações passaram) porque a regra automática de contraste dele só olha **texto**, e aqui não há texto pintado com essa cor. **Conserto**: piso de não-texto (3:1 base vs. `--surface-page` e `--surface-card`, luz e escuro) no gerador, escurecendo PSOL e NOVO com a matiz intacta — respeitando os dois gates já existentes do ADR-0024 (ΔE76 ≥ 12 contra o hex oficial do partido **e** ≥ 12 entre partidos), que é o que torna isto trabalho de design system e não patch de dez minutos. Mais um teste equivalente ao de chip, para o gap não voltar com o próximo partido. **Prazo: antes de 04/10**, não antes de 15/09 — o simulado é teste de tubulação e não tem público. |
| **O gravador do Global Config mandava o Senador para a chave do Presidente** — `cargoFromTseNumeric` (`lib/edge-config/writer.ts`) era literalmente `cargoTse === 3 ? "gov" : "pres"`, escrito quando só existiam dois cargos. Com Senador (5) e Deputado (6) na tabela desde 11/09, **todo payload de cargo 5 ia para `projection-current-pres-t1`**, sobrescrevendo a corrida presidencial com a do Senado: chave válida, JSON válido, eleição errada, **nenhum erro em lugar nenhum**. E `/senador` lê `projection-current-sen-t1` — chave que nunca era escrita: a tela de Senador, entregue em 11/09 com gate de a11y PASS, **nunca teria dado em produção**. | **Crítico** — na noite da apuração a home alternaria entre projeção presidencial real e dados do Senado, conforme qual cron rodasse por último (Senador a cada 5 min) | **Confirmada** (lida no `git show HEAD:lib/edge-config/writer.ts:712-714` em 12/09) | **Fechado em 12/09.** Sai de `cargoToken()` (`lib/config/cargos.ts`), a tabela canônica; código não coberto **lança** em vez de cair num default. Teste de regressão em `tests/unit/edge-config/writer-deputado.test.ts`. ⚠️ **Por que ninguém viu**: `writeEdgePayload` é no-op sem `EDGE_CONFIG_TOKEN` (`writer.ts:211-218`) e o token nunca foi gerado — **nada** jamais foi escrito no Global Config. O defeito estava armado para disparar exatamente no momento em que o usuário completasse a tarefa pendente de colar o token, antes de 15/09. Classe de erro: **default silencioso em conversor de enum** — a mesma do ADR-0028 (`currentCargo` derivando cargo do calendário), que foi removido em 11/09 pelo mesmo motivo, no arquivo ao lado. |
| **Duas cadeiras de 2022 que a aritmética da lei não explica** — MG (NELY AQUINO, PODE) e RS (BIBO NUNES, PL), ambas "ELEITO POR MÉDIA". Nos dois casos o TSE deu a vaga à agremiação de **menor** média, tendo a de maior média candidato acima do piso de 20% — o que o art. 109 I, na leitura literal, não admite. Descartado pelo dado: quociente de MG conferido por duas fontes independentes (210.400, ao voto), votos nominais idênticos entre os dois datasets, e a ordem não muda com ou sem legenda na média. | **Baixo** — 2 de 513 (0,39%), e a fase 1 é exata nas 27 UFs | **Aberto, não explicado** | Hipótese não confirmada: decisão judicial posterior — `DS_SIT_TOT_TURNO` registra o desfecho **jurídico**, que não precisa coincidir com a aritmética. As duas estão **nomeadas** em `test_cadeiras_golden_2022.py::DIVERGENCIAS_CONHECIDAS`, e o teste falha se surgir uma terceira **ou se uma das duas sumir**. Reabrir se o simulado mostrar padrão parecido. |
| **O golden de 2022 não discrimina o arredondamento do art. 106** — quatro UFs (AP, MT, SC, TO) caem na fração exata de 0,5, então a regra **é exercitada**; mas 1 voto de diferença no quociente não move cadeira nelas. Verificado por mutação: trocar `> 0,5` por `>= 0,5` deixa os 7 testes do golden passando. | **Médio** se alguém tratar o golden como prova completa | **Documentado** | Os casos sintéticos de `test_cadeiras.py::test_caso1_*` cobrem a regra, construídos para que 1 voto **mude** a distribuição. O fato está travado em teste (`test_o_golden_exercita_mas_nao_discrimina_o_arredondamento`), que falha se o conjunto de UFs na fronteira mudar. |
| **8,3 GB de dataset do TSE em `build/`** (`votacao_candidato_munzona_2022` sozinho tem 8,1 GB), mais 552 MB do zip solto em `tse_docs/`. Tudo git-ignored. | **Baixo** hoje (16 GB livres), alto se o disco apertar durante o simulado | **Aberto** | O golden commitado tem 232 KB e **não** depende dos CSVs — eles só são necessários para **regerar** a fixture. Podem ser apagados com segurança; `scripts/build-cadeiras-golden.py` diz de onde baixar de novo. |
| **Golden de cadeiras de 2022 — FECHADO em 12/09** | — | ✅ **resolvido** | O dataset `votacao_candidato_munzona_2022` foi baixado à mão pelo usuário (o CDN do TSE responde 403 a cliente automatizado, inclusive com o User-Agent do projeto). Resultado: **511/513 cadeiras**, fase 1 exata nas 27 UFs. Fixture de 232 KB commitada; gerador em `scripts/build-cadeiras-golden.py`. ⚠️ Descoberto no caminho: o cargo 6 **não está** no arquivo `_BR` do dataset de partido — ler a legenda de lá devolve zero em silêncio e custou 6 cadeiras na primeira medição (505/513). |
| **Piso de 20% do QE na 2ª fase das sobras: "candidato" ou "candidato ainda não eleito"?** — o art. 109 § 2º do Código Eleitoral não qualifica, e não há dispositivo nem acórdão que resolva (pesquisa de 11/09). `api/model/cadeiras.py` adota a leitura operacional (**não eleito**), porque senão a vaga ganha não teria a quem ser atribuída. | **Médio** — muda a atribuição da última cadeira de uma UF em cenários apertados | **Aberto** | Travar com fixture do simulado 2 (22–24/09) antes de promover a spec 017. Registrado como open question 1 da spec e no ADR-0027. |
| **Rotas sob *private folder* do App Router respondem 404 em todo ambiente, sem erro** — medido em 11/09: `app/api/_internal/edge-write/route.ts` nunca foi roteável, porque um diretório com prefixo `_` é *private folder* no Next.js e fica fora do roteamento. O comentário original do arquivo declarava a premissa invertida ("rotas começando em `_internal` nunca são expostas publicamente; Vercel não filtra automaticamente"). Consequência: **nenhuma** gravação no Global Config ou no Vercel Blob ocorreu, em nenhum ambiente, desde que o endpoint existe — o caller Python tratava o 404 como warn best-effort e seguia reportando `model_project_ok`. | **Crítico** — era o último elo do pipeline; no dia D o site não mostraria nada novo | **Fechado em 11/09** (commit `0b858b5`) | Pasta renomeada para `app/api/internal/`; o que protege a rota é (e sempre foi) o header `x-model-secret`, mais o BotID de `proxy.ts`. Teste estrutural em `tests/unit/routing/private-folders.test.ts` barra qualquer `route.ts`/`page.tsx` sob segmento com prefixo `_`, e foi verificado que **reprova** ao restaurar o prefixo. |
| **Abort de `listIngestTargets` deixava o lock anti-overlap preso por 6 minutos** — o marcador `running:true` era gravado no passo 3 e o `return` do erro não gravava o `running:false`. Uma falha transitória (a query lê `zonas` no Postgres) custaria **6 ciclos** em vez de 1, no dia D, sem erro visível — só ausência de ingestão. | **Alto** no dia D | **Fechado em 11/09** (commit `0b858b5`) | `releaseOverlapLock` fecha o marcador no caminho de abort; é o único `return` entre os dois marcadores, verificado por varredura. |
| **`historical_results.cod_municipio_tse` é arbitrário — nunca usar como chave geográfica** — o índice único da tabela (`lib/db/schema.ts:47-54`) é `(ano, turno, cargo, uf, cod_zona, cod_candidato)` e **não inclui município**, então o rótulo de município varia entre as linhas da MESMA zona, conforme o candidato. Medido em 2026-09-11: MG zona 4, 2022 t2 c1 — a linha do PT traz `cod_municipio_tse=40177` (Águas Formosas) e a do PL traz `49352` (Fronteira dos Vales). Em MG/2022/t2/c1 há 304 zonas e **457 pares distintos**, para 608 linhas. | **Médio** — qualquer agregação por município que passe por esta tabela produz número plausível e errado, sem erro | **Confirmada** (medida, não estimada) | O ADR-0035 D1 já tirou `historical_results` do `UNION` que montava os pares, mantendo-a só na fonte suplementar B′ restrita a zonas com **um único** município no histórico (`data-pipeline/zonas-import.ts:132-152`) — que é exatamente o recorte onde a ambiguidade não existe. Risco residual: código futuro que releia a coluna sem essa restrição. |
| **A premissa da fatia por município segue sem prova — mas ganhou um terceiro e quarto dataset a favor** — medido em 2026-09-11 sobre `build/tse-archives/votacao_partido_munzona_{2018,2022}`: **6.102 pares** (município × zona) contra **2.636 zonas** reais, o mesmo 6.102 nos dois anos. O TSE declara **6.083** arquivos de zona por cargo para 2026 (`tse_docs/txt/apresentacao-interessados-2026.txt:195-200`) — 0,3% dos pares, e a **2,3× de distância** do nº de zonas: o que o documento chama de "zonas" são pares. Soma-se aos 6.085 do CSV de eleitorado 2024 e aos 6.110 da tabela `zonas`. **Não é prova**: o `cdabr` de um arquivo de zona é só o número da zona com 4 dígitos, sem município (`tse-ea20-arquivo-de-resultado-unificado.txt:180,522-524`), então dois arquivos de pares da mesma zona seriam indistinguíveis pelo conteúdo. | **Crítico** — se cair, `merge_pairs_into_zonas` multiplica votos por até 8× em ~62% das zonas | **Baixa** (quatro linhas de evidência convergentes) | `check_zona_merge_sanity` (`api/model/zona_merge.py`) como rede em runtime, agora com os dois lados do limiar travados em dado real (`tests/unit/model/test_zona_merge_real.py`); e **`pnpm verify-fatia-premise`** (`scripts/verify-fatia-premise.ts`, novo em 11/09) automatiza o Passo 0 do simulado — exit 0 = fatia, 2 = multiplicação, 1 = inconclusivo. **Só o dia 15 resolve.** |
| **Perda de participação (brancos/nulos/abstenção) em 3 de 4 telas de projeção** — o painel de resultado `<ResultPanel>` do design kit atlas-menna não tem campo para participação, só para candidatos. A decisão de adoptar o painel como hero em home (`/`) + UF presidencial (`/uf/[sigla]`) + UF governador (`/uf/[sigla]/governador`) suprime a exibição de brancos/nulos e abstenção nessas três rotas — uma regressão explícita vs. ADR-0018 que recomendava os seis termômetros justamente para NÃO omitir participação (constituição § 8). Dados permanecem no payload (`EdgeParticipacao`/`EdgePayloadUf.participacao`) e exibidos em `/governador` via `<ProjectionThermometers>` obrigatória (ADR-0022). | **Alto** — transparência metodológica é constituição § 8; a omissão é visível e afeta a leitura de "o que está movendo o forecast" | **Confirmada** — decisão do usuário em 2026-09-09 aceitando o custo explicitamente (ADR-0034 D22) em troca de fidelidade ao protótipo | Compensado por `<ProjectionThermometers>` em `/governador` garantindo que **uma** rota sempre exibe participação (ADR-0022); e por `<ForecastTransparency>` em toda página com projeção explicando metodologia (constituição § 8). Não é risco a mitigar — é trade-off aceito. Registrado para memória: não reabrir a decisão sem nova fonte primária. |
| **Rate limit do TSE: bloqueio de IP por 10 min** — máximo de 100 req/s por IP, com bloqueio de 10 minutos **renovado** a cada nova violação durante o bloqueio (`tse_docs/txt/apresentacao-interessados-2026.txt:219-221`) | **Crítico** (bloqueio durante a apuração congela o produto no último valor) | Média | Rate limiter de saída obrigatório (`lib/tse/rate-limiter.ts`), `TSE_MAX_RPS` default **30** — teto de segurança bem abaixo de 100. 429/503 retryáveis honrando `Retry-After` (`lib/tse/retry.ts`). **O valor só é calibrável nos simulados (15–17/09 e 22–24/09)** — hoje é estimativa, não medição, e há apenas duas janelas antes do dia D. Invariante formalizado em [ADR-0020](../architecture/adrs/0020-conformidade-res-23751-2026.md). |
| **Respostas 304 podem contar para o limite de requisições** — o cache condicional (ETag/`If-None-Match`) não isenta a requisição do orçamento de 100 req/s | Alto | Média | **Não confirmado na fonte primária** — o material do TSE não trata de cache condicional. Posição conservadora: contar cada GET condicional no orçamento do rate limiter, como já faz `lib/tse/rate-limiter.ts` (limita requisições, não respostas com corpo). Medir no simulado 1 comparando `rateLimited` observado vs. GETs emitidos. Não relaxar `TSE_MAX_RPS` assumindo que 304 é grátis. |
| **404 por URL malformada pode bloquear o IP** — "Caso os endereços sejam requisitados de forma incorreta (404), também pode haver bloqueio" (`apresentacao-interessados-2026.txt:223-225`); **o limiar não é divulgado** e não há como testá-lo com segurança | **Crítico** | Média | **Proibição absoluta de sondar URL adivinhada** contra `resultados.tse.jus.br` / `resultados-sim.tse.jus.br` (constituição § 1). Toda URL deriva da padronização documentada ([tse-2026-leiautes.md](./tse-2026-leiautes.md) § 1) ou de alvo confirmado por EA11/EA14/EA15. Caminhos de erro (404, 429, `Retry-After`) só se exercitam contra o CDN falso local: `scripts/tse-mock-server.ts` (`pnpm tse:mock`). Nenhum runbook ou teste pode ensinar a forçar 404 contra o CDN real. |
| **Ausência de arquivos de índice** — "Não é possível listar os arquivos" (`apresentacao-interessados-2026.txt:228`). A descoberta de *o que mudou* depende inteiramente de EA14 (Brasil) e EA15 (UF/município) | Alto | Alta | Nomes de arquivo montados deterministicamente (`lib/tse/targets.ts`); direcionamento por acompanhamento (`lib/tse/acompanhamento.ts`, `TSE_ACOMPANHAMENTO`) com **fail-open** — se o EA14/EA15 falhar, busca tudo em vez de perder dado. Leiaute real do EA14/EA15 e o valor efetivo de `f` só se confirmam no simulado de 15/09. Sem índice, um arquivo novo que o TSE passe a publicar é invisível até alguém ler o manual atualizado. |
| **Leiaute sem freeze** — o TSE não anunciou congelamento de schema entre hoje e 04/10; o EA20 é o mais recentemente revisado dos 9 documentos (10/07/2026), sugerindo ajuste fino às vésperas do simulado | Alto | Média | Envelope Zod em **`.passthrough()`, nunca `.strict()`** (`lib/tse/ea20-schema.ts`) — campo desconhecido não rejeita o arquivo inteiro. **Risco residual não coberto**: se o TSE renomear ou remover um campo que o SalaCofre efetivamente consome (`v.vvc`, `s.psa`), o passthrough aceita o arquivo e o pipeline extrai valor ausente **em silêncio**. Mitigação pendente: validação campo-a-campo e alerta de campo obrigatório ausente, além do parse Zod (ADR-0020, Consequências negativas). Re-baixar e diffar os 9 leiautes via `pnpm tse:watch --once`. |
| **O payload não cabe no Global Config** — a Vercel documenta (2026-07-29) **1 MB por store**, do store inteiro, com escrita **recusada** ao exceder. Medido contra o banco em 08/09: 5.572 municípios × ~206 B (custo medido no payload real de MG) = **1,10 MB** para um cargo e **2,19 MB** com Presidente + Governador. `lib/edge-config/writer.ts` afirma 512 KB (número desatualizado) e valida **por requisição**, nunca somando o store | **Crítico** (a escrita é recusada e o site congela no último valor, exatamente na noite da apuração) | **Alta** — e cresce sozinha: hoje só 2.180 dos 5.572 municípios têm dado; o payload aumenta conforme a cobertura melhora | Decisão do usuário em 08/09: **detalhe por município vai para o Vercel Blob**, estendendo a fronteira que o [ADR-0026](../architecture/adrs/0026-cargos-senador-deputado-ingestao-e-read-path.md) já abriu para o Deputado; resumo por UF fica no Global Config. Entra **antes do simulado 1**, para o simulado testar a arquitetura definitiva. Guarda de tamanho do store inteiro no writer, com aviso acionável. |
| **`eleitorado` inflada em 21,8%, de forma desigual por UF** — o CSV do TSE traz `NR_TURNO` e `data-pipeline/eleitorado-import.ts` não filtra: município com 2º turno em 2024 é contado duas vezes. Banco 189.907.728 contra 155.910.528 reais. **SP fator 1,454 · AM 1,526 · MG 1,135 · BA 1,018 · seis UFs 1,000** | **Crítico** — a tabela alimenta o peso de zona, os estratos, o denominador de `pct_apurado` e **o peso de cada UF na agregação nacional** (`api/model/project.py`). O peso de SP contra BA está errado em 43% | Confirmada (medida contra o CSV oficial em 08/09) | **Candidata a causa do gate OT-4, que reprova desde 07/09 e nunca foi investigado nessa direção.** Conserto: filtrar `NR_TURNO = 1` e reimportar — o que muda o modelo, então exige `model-validator` medindo o replay antes e depois. |
| **`eleitorado` não é desagregável por município** — a tabela é chaveada por zona e o importador guarda "o primeiro município visto", mas **1.640 de 2.648 zonas (62%) cobrem de 2 a 8 municípios**. Somando pelo banco, Água Comprida (~2.000 habitantes) sairia como 8º maior colégio eleitoral de MG com 172.857 eleitores; Montes Claros e Uberaba sumiriam. Só 2.154 dos 5.572 municípios têm eleitorado atribuído | Alto (qualquer painel por eleitorado municipal publicaria lista falsa; a tabela `top-by-eleitorado` de `/uf/[sigla]/governador` renderiza **zero linhas** desde que foi escrita, por isso) | Confirmada em 08/09 | O dado municipal correto está no mesmo CSV (`CD_MUNICIPIO` por seção). Precisa de onde morar — coluna em `municipios` ou tabela nova — e de migration + import. Até lá, nenhum painel por eleitorado municipal deve ser publicado. |
| TSE fica indisponível por >5min | Alto | Média | Graceful degradation com último valor; banner amarelo |
| Modelo retorna projeção absurda em t=início | Alto | Média | Penalização forte de CI <5% apurado; guardrails de sanidade |
| Pico de tráfego excede 20k | Médio | Média | Edge Config + CDN escalam automaticamente; load test 30k |
| Bug de renderização em mobile específico | Médio | Média | E2E em Playwright com BrowserStack; bug bash em Set |
| Equipe pequena vs escopo grande | Alto | Alta | Priorizar M sobre S/C; cortar governadores se necessário |
| Atribuição partidária problemática | Médio | Média | Disclaimer explícito; cores neutras se candidatos novos |
| Custo de Vercel acima do orçado | Médio | Baixa | Monitorar consumo semanal; alertas de billing |
| ~~Bundle JS estoura RNF-007a (>150KB above-the-fold)~~ → **REAVALIADO em 2026-09-05**: o número estava **inflado por artefato de medição**, não por código. A soma incluía o chunk de polyfills legados que o Next emite com `nomodule` (**39.373 bytes gz**, idêntico a `next/dist/build/polyfills/polyfill-nomodule.js`) — navegador que entende `<script type="module">` **nem faz o request**. A própria medição da S05 o contava: os "168KB" eram 71+**40**+39+18, e aquele bucket de 40KB é este mesmo polyfill. Descontado, o custo real é de **~148,7 KiB em 8 requests** — **dentro da meta**, com o veredito dependendo de arredondamento (KB decimal vs. KiB). Nenhum chunk contém `framer-motion`, `zustand`, `swr`, `zod`, `maplibre` ou `d3-*`: o código do app não vazou; o que resta é overhead de framework. | Baixo | Reavaliado | **Ação: consertar a régua, não o código.** Antes de virar gate de CI, a medição precisa filtrar `noModule` — de preferência capturando requests reais via Playwright, que já aplica a semântica de `nomodule`. Receita em [performance.md](../nfr/performance.md#-como-medir-rnf-007a-sem-inflar-o-número-2026-09-05). **Não** perseguir `browserslist` para cortar polyfills: como o chunk nunca é baixado, removê-lo não melhora LCP nem INP — só limparia o número. Resta explicar ~18KB de diferença nos buckets de router/RSC entre a S05 e hoje, provavelmente versão do Next; não é prioridade. |
| **Modelo: validação dinâmica pendente até simulado oficial TSE 2026** | **Alto** | **Média** | **Spec 002 em `implementing` (não shipped). Gate técnico OT-4 PASS (MAE@1h <2pp) mas dataset T21 era circular → swing efetivamente zero. Re-rodar `model-validator` com dado dos simulados oficiais (**15–17/09** e **22–24/09**, janelas 9h–12h e 14h–17h BRT); aceitar relaxar OT-4 via ADR só se 3 tentativas falharem. Vide retrospective S03 (Carry-over #11) e `spec.md ship_blocked_on:`** |
| **DF sem eleitorado em 2026** | Médio | Alta | Tabela `eleitorado` 2026 não tem DF; modelo silenciosamente exclui DF do MAE e da agregação nacional. Watch ativo no TSE; quando dataset publicado, rodar `data-pipeline/load-eleitorado.ts --uf=DF`. Carry-over #5 da S03. |
| **`tse.lag_seconds` acima da meta S07 em granularidade `zona`** — meta S07 (`docs/testing/tse-simulados.md`) é **< 90 s**; com `TSE_GRANULARIDADE=zona` (necessária desde 05/09 — o modo `uf` quebra o modelo, ver risco de "Orçamento do ciclo" abaixo) o ciclo de ingestão sozinho já mede **~106–160 s** (ensaio de escala de 05/09 contra mock local, ~5.302 GETs/ciclo, 2 cargos), e o cron de 60 s + lock anti-overlap de 3 min produz cadência efetiva de **~120 s** — projetando lag em regime permanente na faixa de **~130–160 s** | Médio (degrada a "frescura" percebida do produto, não quebra a correção) | Alta (decorre de aritmética medida, não de falha esporádica) | **Aceito pelo usuário na decisão E4** (plano de projeção por regra de três, 2026-09-05) como custo conhecido do trade-off `uf` quebrado vs. `zona` funcional — não é um risco a mitigar às cegas, é medir. **Ação**: re-medir com o CDN real no simulado 1 (15–17/09); se o número persistir acima de 90 s, avaliar Z2 (shards) ou otimizar o Postgres do ciclo (2 SELECT + 1 INSERT por zona mudada hoje, sem batch — ver [runbook § Ensaio de escala](../operations/runbook.md#ensaio-de-escala-05092026--mock-local-3-ciclos-completos)) antes de mudar o contrato do ciclo. Não relaxar a meta de 90 s via ADR sem tentar essas duas alternativas primeiro. |
| **Orçamento do ciclo `/api/ingest` vs. granularidade que o modelo exige** — *(reescrito em 2026-09-05; a versão anterior dizia "~73.000 targets/ciclo", errado por duas gerações)* | Médio | Alta | **Números corretos** ([tse-2026-leiautes.md](./tse-2026-leiautes.md) § 6): com granularidade de **zona** eram ~2.600 GETs por cargo (~5.200 para Presidente + Governador), **não 73k**; com `TSE_GRANULARIDADE=uf` (novo default) são **~55 GETs/ciclo** (27 UFs × 2 cargos + 1 BR), que cabem folgadamente em 60s dentro do rate limiter. **Trade-off que continua aberto**: o agregado de UF vem pré-agregado pelo TSE e **não contém dados por zona**, mas RF-011/012 (spec 002) exigem swing zona-a-zona vs. 2022 — não dá para o modelo operar só com UF sem violar RF-011 como está escrito. Desenho recomendado: **híbrido** — UF/BR para as telas (specs 003/004), zona apenas nas UFs que o EA14 sinalizar como alteradas (`TSE_GRANULARIDADE=zona` + `TSE_ACOMPANHAMENTO=on`). **A decisão é do usuário, após medir latência e volume no simulado de 15–17/09** — nenhum agente deve fechá-la antes disso. Carry-over #14 da S03, reaberto com escopo corrigido. |
| **Drizzle baseline desconectado do Neon** (`pnpm db:generate` gera CREATE TABLE de tudo) | Baixo | Alta | Migrations S01/S02 foram manuais (`lib/db/migrations/0001_postgis.sql`, `0002_zonas_pk_fix.sql`); drizzle nunca viu o DB. Reconciliar via `drizzle-kit introspect` + baseline marcado como applied. Não bloqueia operação — afeta DX. Carry-over #10 da S03. |
| **Vercel preview deploy do Python (Fluid Compute) ainda não validado end-to-end** | Médio | Média | T02 da S03 ficou parcial: smoke local OK (`python3.14` standalone), mas `curl https://<preview>/api/model/project` precisa de `vercel deploy` com creds owner. Handoff operacional pendente. Risco: descobrir gotcha de runtime tarde. Mitigação: rodar deploy preview cedo na S04 antes de UI consumir Edge Config. |
| **Chunk MapLibre estoura RNF-007b (<250KB gz)** | Médio | Alta | `pnpm build` em 2026-05-17 pós-map-builder reportou chunk lazy do mapa em **287KB gzipped** (1.07MB raw) — só `maplibre-gl` consome isso, `pmtiles` parser é apenas ~30KB. S06 reportou **281KB gz** (melhora 6KB). Lazy-loaded (não conta pro RNF-007a above-the-fold da home), mas viola a meta de RNF-007b. Opções catalogadas pra `a11y-perf-auditor`: (a) atualizar RNF-007b pra 300KB via ADR (maplibre moderno é gordo, não há alternativa Vercel-nativa mais leve); (b) code-split entre features do MapLibre (só carrega o que usa); (c) trocar pra Leaflet (perde GL acceleration). Recomendação: (a) — overhead aceitável vs trade-off de feature loss. Validar com bundle-analyzer (`ANALYZE=true pnpm build`). <br>⚠️ **Entrada desatualizada em dois pontos (constatado 2026-09-18, não reescrita para preservar o histórico da decisão)**: (1) o título ainda diz `<250KB gz`, mas a opção (a) foi adotada — `docs/nfr/performance.md:20` fixa RNF-007b em **<300KB gzipped**, e a medição atual é 285,3 KiB, **dentro** da meta; (2) `ANALYZE=true pnpm build` **não funciona** neste repositório — ver o meta-risco no fim desta tabela. O risco vivo hoje é a folga compartilhada por três rotas, registrado em entrada própria abaixo. |
| **Edge Config externo `ecfg_mcoa3usgvm5dbqb27vae8ptmpdxl` inacessível ou expirado** | Médio | Confirmado em 2026-05-18 | Smoke local pós-S06: cada page-load esperava ~10s antes do SDK `@vercel/edge-config` desistir. Mitigação imediata: `EDGE_CONFIG` comentado em `.env.local` (dev pega fixture sintética). Pendente: descobrir se Edge Config foi deletado ou token expirou; eventualmente reativar ou recriar. Carry-over S07. |
| **`readProjection` em `lib/edge-config/reader.ts` sem AbortController timeout curto** | Médio | Alta (recorrente em dev) | Quando `EDGE_CONFIG` está setada mas endpoint não responde, cada page-load espera ~10s. Endurecer o reader com `AbortController` (~1s em dev, ~5s em prod) elimina o problema sem precisar tocar env vars. Chore S07. |
| **`SLACK_WEBHOOK_URL` não está configurada — alarme de dado parado inerte até que seja** (ADR-0038 D3) | Baixo | Média | O alarme de pipeline parado (`api/model/project.py:_alert_slack`, acionado quando `now() - dado_ts > limiar_cargo`) está **implementado**, mas `_alert_slack` retorna silenciosamente quando `SLACK_WEBHOOK_URL` não está em `os.environ`. Antes de qualquer corrida real (simulado 15/09 ou 1º turno 04/10), configurar a variável em Vercel Settings → Environment Variables → Production/Preview com a URL do webhook Slack (ou outro sistema de notificação). Sem isso, o alarme segue rodando a cada ciclo, sendo logado, mas nunca viajando para fora dos logs estruturados. |
| **CSV mesorregião IBGE 2022 não populado** | Baixo | Alta | Migration 0005 (S06 Fase 2) criou schema `mesorregioes` mas população skipped — CSV `docs/ibge-2022/municipios-mesorregiao.csv` ausente. UI degrade gracioso (bloco "Apuração por mesorregião" em `/uf/[sigla]/governador` fica oculto). Owner-action: pull `RELATORIO_DTB_BRASIL_MUNICIPIO_2022.xls` do IBGE FTP, converter pra CSV (4 colunas: cod_municipio_ibge, cod_meso, nome_meso, uf), commitar, re-rodar `pnpm tsx data-pipeline/migrations/0005_municipios_mesorregiao.ts`. Esperado: 5.570 municípios → ~137 mesorregiões. |
| **Replay 2T 2022 não suportado em `scripts/replay-2022.ts`** | Médio | Alta | Harness atual valida só replay 1T (S03 + S05 estendido pra 11 cands). Pra validar mudanças S06 do modelo (`p_passa_2t`, `p_fecha_1t`, `cenarios_2t`) em replay real precisa estender o script pra turno binário. Estimado ~10h. Carry-over S07. |
| **Fixture sintética `cargo=gov` ausente em dev** | Baixo | Alta | `/governador` e `/uf/[sigla]/governador` exibem só shell em dev (degrade gracioso correto). Criar `tests/fixtures/edge-config/gov-current.json` + `archive-pres-t1.json` (~300 linhas) destrava smoke visual completo pré-D1. Carry-over S07. |
| **Gate OT-4 (replay 2022) FAIL** — MAE@1h acima da meta | Alto | Alta | Replay regenerado com método novo (regra de três por zona, ADR-0021) reprovou: MAE@1h de 3,225pp (Lula) e 2,609pp (Bolsonaro) contra limiar de 2pp; cobertura IC95 em 1h de 78,8% contra meta de 90%. Converge para 0,3–0,4pp em 2h. Causa diagnosticada: **viés de composição** — as zonas que reportam primeiro (maiores, urbanas) não representam a UF, e bootstrap não mede esse viés (risco já catalogado no próprio ADR-0021 como central ao método). **Tentativa 1** (corrigir o denominador de `pct_apurado`) não moveu o gate — aquele valor só alimenta o gatilho binário de RF-018 (`< 5%`), que em t=1h não dispara para UF nenhuma; corrigiu, porém, um bug real de produção. **Tentativa 2** (pós-estratificação por tercis de porte de zona, [ADR-0023](../architecture/adrs/), 06/09): MAE@1h caiu para **2,590pp** (Lula) e **2,070pp** (Bolsonaro) — 20% de redução no ponto — mas a cobertura ficou em **79,5%**, praticamente parada. Leitura: se o ponto melhora e a cobertura não, a incerteza residual não está *entre* estratos e sim **dentro** deles (as zonas grandes que reportam primeiro não representam as outras grandes), somada à apuração parcial intra-zona; o bootstrap não vê nenhuma das duas porque reamostra as zonas que já reportaram. Regra da casa: relaxar limiar via ADR só após 3 tentativas falharem — **resta a tentativa 3**. ⚠️ **Ressalva epistêmica**: a ordem de apuração do replay é **sintética** (zonas por porte + atraso N/NE de 3 timesteps) porque a ordem real de 2022 sumiu do CDN; o MAE mede o erro sob a hipótese de viés que nós mesmos escolhemos, e o limiar de 2pp nunca teve base empírica (foi herdado de quando o gate era tautológico). **Bloqueia promover spec 002 a `shipped`** — registrada em `ship_blocked_on` da spec 002. Caminhos para a tentativa 3: (a) estratos mais finos (quintis) ou segunda dimensão geográfica, (b) atacar a largura do intervalo para consertar a cobertura sem mexer no ponto, (c) recalibrar o limiar via ADR com a ordem de apuração real medida nos simulados (15–17/09 e 22–24/09). **Atualização 2026-09-11 (ADR-0033 § 3, Decisão 3)**: dado real de 2022 confirmado inexistente — a calibração vira **faixa de sensibilidade**, não um ajuste de parâmetro. Implementada em `scripts/replay-sensitivity.ts` (`pnpm tsx scripts/replay-sensitivity.ts`), documentada em [docs/testing/replay-sensitivity.md](../testing/replay-sensitivity.md); o gate oficial continua sendo `delay=3` em `scripts/build-replay-fixtures.ts` (agora parametrizável via `REPLAY_REGIONAL_DELAY`, default 3) e continua **FAIL**. |
| **Reimportação de candidatos obrigatória em 02–03/10 — janela crítica sem mitigação** | Alto | Alta | ADR-0040 especifica a cadência: diária até ~20/09, 2–3 dias até 01/10, **obrigatória** em 02–03/10 (última confirmação antes da apuração). O script `pnpm candidatos:import` é manual, sem cron automático; sem execução nesse período, a lista de candidatos congelaria na versão de 20/09 enquanto o TSE continua recebendo candidaturas (recursos, substituições). RF-152 implementa a guarda de encolhimento (aborta se cair >2% vs. publicação anterior, evitando truncações acidentais), mas não automatiza a execução. **Ação operacional**: calendário no Slack/sheet com lembretes em 02/10 e 03/10, ou cron simples (fora de escopo S07) que rode `candidatos:import --force` naquele período e dispare alerta se falhar. Risco de operação, não de código. |
| **Visão municipal cobre só ~39% dos municípios, e a interface não avisa** | Médio | **Alta (é o estado atual)** | `EdgeUfMunicipio` é derivado agregando os snapshots de **zona** (`fetch_municipio_aggregates`), e a tabela `zonas` liga cada zona a **um** município — a sede (`cod_municipio_tse`). Uma zona rural cobre vários municípios, e a agregação atribui **todos** os votos da zona ao município-sede. Medido em 07/09: **2.180 de 5.572 municípios** são sede de alguma zona; por UF, MG 272/853 (32%), SP 269/645 (42%), RS 146/499 (29%), SC 89/295 (30%). Os demais não recebem linha em `municipios[]` e ficam sem cor de dado no mapa, com seus votos somados ao vizinho-sede. **Não é regressão da granularidade de zona** — ao contrário, no modo `uf` não havia recorte municipal nenhum; é limitação estrutural do mapeamento zona→município, anterior ao ADR-0021. Mitigação mínima (barata, recomendada antes de 04/10): rótulo honesto na interface declarando que o dado municipal é agregado por zona-sede — hoje o leitor não tem como saber, o que é dívida sob a constituição § 8. Mitigação completa (pós-simulado): ingestão EA20 em granularidade de município com **cadência própria, mais lenta** — ~11.140 GETs/ciclo não cabem em `maxDuration=180` (≈223 s a 50 rps), mas em sharding por terços do país cada município atualizaria a cada ~3 ciclos (~6 min) sem afetar a cadência de UF/nacional. Custo real: `TSE_GRANULARIDADE` só aceita `"uf" | "zona"` e `snapshots` é chaveada por `(uf, cod_zona)` sem coluna de município — é trabalho de ingestão e schema, não uma flag. Ver specs [008](../specs/008-interatividade-brushing/) e [015](../specs/015-drill-down-municipio/). |
| **RF-017 não dispara para UF sem nenhuma zona apurada** | Médio | Média | Uma UF que não tem **nenhuma** linha em `snapshots` (nunca processada por `compute_uf_projections`) nunca entra em `pending_national_fallback`, não é imputada pelo nacional e **desaparece de `uf_rows`** — em vez de receber a imputação nacional (±10pp) que RF-017 promete. Não afeta replay 2022 (todas UFs têm ≥1 zona em t=1h), mas é lacuna real nos primeiros minutos de 04/10, quando UFs inteiras ainda não reportaram nenhuma zona. Reprodutor: teste `test_zero_zonas_retorna_none` está em `test_extrapolation.py` e passa, mas é só para 0 zonas; comportamento agregado (uma UF inteira ausente de snapshots ainda não coberto). Ação: (a) adicionar teste em `test_pct_apurado_denominador.py` ou `test_edge_cases.py` que construa `uf_rows` com algumas UFs presentes e algumas ausentes, validando que a ausente recebe `metodo: imputado_nacional` com IC ±10pp; (b) se teste falhar, arrumar `compute_uf_projections` ou `build_edge_payload` para não silenciar UFs-fantasma. Bloqueador: nenhum (é comportamento futuro, não regressão — não afeta D1 em 04/10 13h quando todas UFs têm ≥1 zona). |
| **Capacidade — redesign + specs 016/017 (Senador/Deputado) + tentativa 3 do OT-4, 17 dias até D1** — S07 S06 ganha Fases 7 (redesign UI) e 8 (cargos 5/6), a somar com Fases 0–6 do pipeline. Parallelismo limitado por CI/preview Vercel + capacidade individual. OT-4 (gate de modelo) travado em tentativa 2 (MAE@1h em ~2.5pp contra limiar 2pp). Specs 016/017 dependem de ADR-0026 (crons/Blob) e 0025 (design system) completados. | **Alto** | **Média** (cenário planejado, não falha aleatória) | **Prioridade do usuário**: P0 pipeline/simulado › P1 redesign › P2 Senador › P3 Deputado. Specs 016/017 desabilitadas na navegação até shippar (não bloqueiam D1, rodas fracas). Tentativa 3 do OT-4 (estratos por porte + revisão de método, ADR-0023) a rodar no simulado 1 (15–17/09); relaxar limiar via ADR só se 3 tentativas minguarem. Não reabrir escopo redesign a menos que falhe no simulado 2. |
| **`lib/config/calendar.ts` tipado para uma corrida ativa por vez, incompatível com 4 cargos simultâneos de 1T** — arquivo é `type Cargo = "pres" | "gov"` (ADR-0012) e `currentRace()` retorna um único cargo. ADR-0026 introduz `"sen"` e `"depfed"`, mas função assume exclusividade. Em 04/10 a partir de 8h (urna abre), **4 cargos votam simultaneamente**: presidente, governador, senador, deputado federal — e o UI precisa de multi-abas/toggle por cargo. Código rejeita qualquer tentativa de cargo fora dessa lista; qualquer lógica que dependa de "há uma corrida ativa" desaba. | Médio (regressão em routing de telas, não correção) | **Alta (é o estado atual do código)** | Estender `calendar.ts` para suportar array `activeRaces: Cargo[]` ou `activeCargosByRegion`. Design debate: centralizador vs. caótico. Estimado ~4–6h de design+tests. **Bloqueador de spec 016 (Senador)**: registrado em `ship_blocked_on:` da spec até solução implementada. Janela: antes ou após D1, depende se Senador entra em produção antes de 04/10 (planejado em S07, mas pode defer pra S08 se apertar). |
| **Orçamento Edge Config 512KB com 4 cargos do 1T (Pres + Gov + Senador + Deputado)** — payload nacional hoje ~75KB (Pres); Governador +~5KB; Senador +~1KB por UF (~27KB nacional); Deputado +~270–405KB por UF (exclusão ao ADR-0001 via Blob). Soma: ~75+5+27 = ~107KB payload central + 27×(~2–4KB UF) = ~161KB total, **dentro do limite** só se Deputado viver em Blob (ADR-0026). Cenário de falha: se Deputado for forçado a Edge Config (ex.: Blob indisponível), o teto de 512KB é quebrado. Medição de hoje é extrapolação de padrões antigos (Pres 1.5 anos, Gov ~100 dias); números reais de Senador e esp. Deputado só confirmam no simulado 1. | Médio | Média | **Aceito em ADR-0026**: Deputado vive em Vercel Blob, não Edge Config, como única forma de caber. Senador permanece em Edge Config. **Ação**: (a) re-medir payload no simulado 1 com dado do TSE; (b) se margem ficar apertada (<50KB folga), aplicar compressão (zstd no Vercel, decompress no reader), remover campos secundários de `candidates[]` ou postergar cargos 5/6 para pós-D1. Invariante: **nunca** Postgres no read path (ADR-0001). |
| **A premissa da "fatia por município" do EA20 por par nunca foi verificada contra dado real** — desde a migration 0006/ADR-0035, a ingestão busca um arquivo EA20 por par `(município, zona)` (`<uf><mun5>-z<zona4>-c<cargo>-e<n>-u.json`), e `api/model/zona_merge.py::merge_pairs_into_zonas` soma os pares de volta à zona antes do estimador. A arquitetura inteira assume que cada arquivo traz só a **fatia** da zona que cai naquele município — nunca a zona inteira. Evidência a favor (três linhas convergentes, nenhuma decisiva): o nome do arquivo **exige** um município (`tse_docs/txt/tse-ea20-arquivo-de-resultado-unificado.txt:118-123`); o TSE declara **6.083** arquivos de zona por cargo (`tse_docs/txt/apresentacao-interessados-2026.txt:195-200`) contra ~2.619 zonas reais, batendo com os 6.085 pares município×zona medidos no CSV de eleitorado 2024; e o EA16 aninha `sec[]` dentro de `zon[]` dentro de `mu[]` (`tse_docs/txt/tse-ea16-*.txt:44-70`), de modo que uma seção pertence a exatamente um par. Evidência contrária: a descrição de conteúdo do próprio EA20 diz "na abrangência da **zona eleitoral**" (`:99`), sem qualificar. Se a premissa cair, a soma multiplica os votos de cada zona pelo número de municípios que ela cobre (até 8×) em 62,5% das zonas — invisível em qualquer teste com fixture sintética (uma linha por zona). | **Crítico** (se falsa, o modelo inteiro conta votos e eleitorado errado em 62,5% das zonas do país, inclusive na noite da apuração) | **Desconhecida — não medida, apenas inferida de três indícios indiretos** | Guarda de sanidade em runtime, `api.model.zona_merge.check_zona_merge_sanity` (chamada em `_do_project` logo após `merge_pairs_into_zonas` e `fetch_eleitorado`): compara `Σ e.te` dos pares de cada zona multi-par contra o eleitorado da zona em `eleitorado`; razão dentro de 0,5–1,5 é silenciosa (ruído esperado de cadastro 2024 × eleição 2026, incluindo a inflação desigual por UF já catalogada nesta tabela), razão ≥ 1,8 loga `error` estruturado (UF, zona, nº de pares, razão) e aciona Slack (`_alert_slack`, novo — não existia canal Python equivalente a `lib/tse/alerts.ts`) — sem abortar o ciclo (§ 7). Isso torna a violação ruidosa, não a impede. **Resolução real**: protocolo do simulado de 15/09 (`docs/testing/tse-simulados.md` § "Passo 0") baixa os arquivos de uma zona multi-município real e faz a aritmética decisiva (Σ dos pares vs. eleitorado da zona) antes de qualquer outro passo do dia. |
| **Leitor entende o placar zerado como "ninguém pontuou"** (R1 da spec 019) | Alto | Baixa após mitigação | Fase pré mostra identidade de candidatos com zeros de voto — e zeros desenham a **forma** de um resultado. Mitigações: faixa não dispensável no topo (RF-160), `<h1>` reformulado (RF-161), vocabulário de medição suprimido, ordem por número de urna (não rank de pct zerado), `poles={false}` no painel (RF-155). Todas reduzem probabilidade, **nenhuma elimina o risco residual de um leitor que rola até o placar e salta a faixa**. **Risco explicitamente ASSUMIDO pelo dono do produto em 2026-09-13**, após a objeção ter sido levantada por escrito e a decisão de produto mantida. Registrado aqui para que ninguém descubra isso como surpresa daqui a três semanas. |
| **`emptyPayload()` de `/governador` e `/senador` cai em modo normal quando Global Config falha** (R7 da spec 019) | Médio | Média | ✅ **FECHADO 2026-09-14**: `emptyPayload()` foi removido; as duas páginas agora montam um ramo de espera de verdade com `FasePreEleicaoBanner variante="sem_dados"` + `UfLinksGrid`. Não bloqueia mais promoção de spec 019 a `shipped`. |
| **Incidente operacional 2026-09-14 02:10 UTC — dev server em produção corrompeu dados** | Crítico | Muito baixa (ocorrência única, agora prevenida) | Em 14/09 02:10 UTC o site público exibiu resultado falso ("CANDIDATO 100 — 55,0% — 100% apurado", três semanas antes do pleito). **Causa**: `pnpm dev` de pé na porta 3000 carregando credenciais **de produção** de `.env.local` recebeu POSTs que a suíte de testes dispara ao fim do orchestrator Python (`_resolve_internal_base_url` cai em `http://localhost:${PORT:-3000}` quando `INTERNAL_BASE_URL` não está definida). Nove POSTs, nove respostas 200, seis chaves de dado sintético gravadas em produção. **Defesas instaladas (três camadas)**: (a) `tests/setup/no-remote-writes.ts` — suite aborta antes de POST para localhost; (b) `conftest.py` autouse `pytest` — Python também rejeita remotes em dev; (c) `app/api/internal/edge-write/route.ts` — 403 quando `NODE_ENV=development` (válvula `ALLOW_DEV_EDGE_WRITE=1` só pra debug local). **Verificado por experimento, não por dedução**: o MESMO POST autenticado devolveu 403 sob `next dev` e **não** devolveu sob o output buildado (`next start`). A razão é estrutural e não depende de configuração de ambiente: `next build` fixa `NODE_ENV=production` no output, e **nenhuma deployment da Vercel roda `next dev`** — nem produção nem preview. É isso que separa esta guarda da do ADR-0032 (`EDGE_CONFIG_STORE_GUARD_BYTES`), recusada de propósito: aquela decidiria por uma **medição que varia** e podia cruzar o limiar em produção sem ninguém prever; esta depende de uma condição que é **falsa por construção** em qualquer servidor publicado. **Regra operacional**: nunca rodar a suíte com servidor em dev na porta 3000. Edge Config foi limpo (backup em `scratchpad/edge-config-backup-14set-0215Z.json`). Lição: um fallback de conveniência que monta endpoints é pior que nenhum — e um `resolve_url()` que silenciosamente cai em `localhost` é a armadilha. |
| **Rota `/senador` (nível Brasil) não tem cobertura de CI — ausente de e2e perf + a11y** (S08, ADR-0048) | Médio | Alta | A superfície nova (`<NationalChoroplethMap>` no nível Brasil de Senador) ganhou junto com Governador no commit `6b40f8b`, mas a suite de e2e que valida RNF-007 (perf) e RNF-025 (acessibilidade) não foi estendida para a nova rota. Missing: `tests/e2e/perf-budget.spec.ts:76` (adicionar `/senador` ao array `routes`) e `tests/e2e/a11y-audit.spec.ts:38-53` (adicionar `'/senador'` ao array de testes). Sem portão, mudança no mapa (cor, tipografia, SVG embutido) quebra RNF sem alarme. `/uf/[sigla]/senador` está fora de escopo por desenho (spec 016 § Escopo/Fora — sem mapa municipal), mas `/senador` é parte do requisito. |
| **`aria-describedby` do mapa descreve diferente entre cargos** (S08, ADR-0048) | Médio | Média | Em `/governador` o mapa **não emite** `aria-describedby` (o valor é `undefined`, o atributo não chega ao DOM) — é o comportamento correto, e deliberado: o `id` `state-grouped-table-heading` só existe em `/`, e apontar para id inexistente seria pior. Mas significa que o mapa de Governador não tem equivalente textual anunciado. Em `/senador` o alvo existe (`corridas-heading`), porém o texto anunciado é só "Estado a estado" — não a lista de 27 corridas, porque `<Panel>` (`components/atoms/surfaces/Panel.tsx:126`) coloca o `id` só no heading `<h2>` e o corpo do painel é irmão dele, não descendente; `aria-describedby` computa a descrição a partir do nó referenciado, então a lista fica de fora. RNF-025/WCAG SC 4.1.2 (Name, Role, Value) exige que o nome acessível da região corresponda ao que a superfície visual apresenta. Divergência: Senado está registrada em ADR-0048 item 5 como completa (rótulo visual "· 2 vagas" + `aria-label="... Senado: 2 vagas por estado"`), mas o `aria-describedby` alvo raso fica como risco residual se não for expandido. |
| **Mapas de Governador e Senador sem legenda** (S08, ADR-0048) | Médio | Baixa (design aceito) | `buildCandidateLegendEntries()` (`components/blocks/NationalChoroplethMap.tsx`) retorna `null` para cargo 3 e 5, porque `rank` reinicia por UF nesses cargos — legendar "1º, 2º, 3º" com as cores correspondentes é impossível quando há 27 candidatos com `rank === 1`. O docstring já diz "melhor nenhuma legenda do que uma incorreta", e nessa escolha quem enxerga depende só de cor fica sem referência (WCAG 1.4.1 Use of Color). Sem alternativa visual — o painel textual (`<StateResultSheet>`) é o substituto semântico. ⚠️ **Sem mitigação hoje.** Medido em 18/09: a string "cores por partido" não aparece em componente nenhum, e os `aria-label` do mapa dizem "UFs coloridas por projeção" — que não explica o código de cores. Quem depende de cor tem como única saída abrir a ficha de cada estado (`<StateResultSheet>`), uma por vez. Uma legenda **de partidos** (não de rank) é possível em gov/sen, porque `top_candidatos[].partido` existe por UF; o que não é possível é a legenda por rank. Candidata a chore. |
| **Orçamento RNF-007b (chunk MapLibre 300KB) agora compartilhado por três famílias de rota** (S08, ADR-0048) | Médio a alto (após primeira violação) | Média | Chunk do MapLibre medido em **285,3 KiB de 300** (95,1% da capacidade), **14,7 KiB de folga**. Antes desta mudança (`6b40f8b`), a pressão vinha só de `/` + `/uf/[sigla]` (Presidente). Agora três famílias competem pela mesma folga: qualquer crescimento de `NationalChoroplethMap`, `party-color.ts` ou `margem-senado.ts` quebra `/governador` **e** `/senador` simultaneamente, risco que antes afetava apenas uma rota. Mudança de build nesta sessão (+88 B de `margem-senado.ts`) não estourou; próxima mudança (token de cor novo? novo campo do mapa?) pode. Mitigação: monitorar com `ANALYZE=true pnpm build` **antes** de cada merge que toca mapa (⚠️ comando documentado mas não funciona — ver próximo risco). |
| **Ferramenta de análise de bundle `ANALYZE=true pnpm build` não funciona** (S08, meta-risco) | Médio (afeta confiabilidade da medição anterior) | Alta (estrutural) | O comando é documentado em `CLAUDE.md § 12` ("Modelo e gate") e em `docs/nfr/performance.md:94` como o método de validar RNF-007a/b/c; na verdade `@next/bundle-analyzer` não está em `package.json`, nem em `next.config.ts`, nem em `node_modules` — verificado em 18/09 18:53. Comando roda um build normal, exibindo progresso de bundle idêntico ao sem `ANALYZE`, criando ilusão de medição. **É pior que falhar**: um erro teria aviso claro; um silêncio enganoso acumula. Ação de curto prazo: (a) instalar o plugin `@next/bundle-analyzer`, ou (b) desmentir a documentação e oferecer alternativa (`next build --analyze` nativo, se suportado em next@16, ou script em Python). Ação de longo prazo: gate de CI que executa análise de bundle antes de PR, falhando se chunk > 300KB. |

## Riscos fechados

Linhas preservadas para histórico — **não reabrir sem fonte primária nova**. Todos os quatro fechamentos abaixo
decorrem da pesquisa em fonte primária de 2026-09-05, formalizada em
[ADR-0020](../architecture/adrs/0020-conformidade-res-23751-2026.md) e refletida em
[regulatory.md](./regulatory.md).

| Risco (texto original) | Fechado em | Por quê |
|---|---|---|
| ~~A suíte de testes escrevia no banco de PRODUÇÃO~~ — cinco testes de integração (`model-edge-cases`, `model-cycle`, `ingest-cycle`, `ingest-model-trigger`, `ingest-routes-auth-cargo`) inserem linhas reais, e o **único** freio era a ausência de `DATABASE_URL`. Em 2026-09-17 isso falhou do jeito mais banal: alguém carregou o `.env.local` para conferir uma migration e rodou `npx vitest run`. Resultado medido no banco: **1.877 linhas de harness**, sendo 1.233 sob cargos que não existem (91, 92, 93) e **644 sob o cargo REAL 1**, com as candidaturas sintéticas 101 e 102 ao lado das de verdade. | **2026-09-17** | Duas causas, as duas fechadas. **(1)** A guarda era a *ausência* de uma variável, e ausência se perde por acidente: agora `tests/integration/_guarda-banco.ts` é ponto único e exige `ALLOW_DB_WRITE_TESTS=1` **declarado** — a dúvida resolve para "não escreve". Não se detecta "é produção" pelo host de propósito: adivinhação com default permissivo é rede de mão única, e uma URL nova de preview reabriria o buraco. **(2)** O cleanup filtrava `uf IN (…)` e **`IN` nunca casa com `NULL`** — as linhas de escopo NACIONAL, que são as que o modelo cria em todo ciclo, jamais eram apagadas; o teste limpava o que via e deixava o que não via, por meses. Agora é `(uf IS NULL OR uf IN (…))`. **Prova**: suíte completa com `.env.local` e sem a autorização → `projections` 13.258 → 13.258, `snapshots` 921 → 921, 21 testes pulados. ⚠️ Nenhum dado real foi apagado em momento algum — os `DELETE` desses testes são filtrados por UF sentinela, faixa de zonas e marcador próprio. **Lição que vale além deste caso**: um `DELETE … WHERE col IN (…)` de limpeza só apaga o que a coluna enxerga; se a coluna é anulável e o `NULL` significa alguma coisa, o furo tem o tamanho desse significado. |
| ~~Cadastro TSE atrasado~~ (Crítico / Baixa — "Iniciar em Jun 2026, deadline tipicamente Set") | **2026-09-05** | **Não existe cadastro.** A Res. 23.751/2026 (arts. 264–269) não institui inscrição, credenciamento ou homologação de "interessado na divulgação" — as strings `cadastr`, `credenci` e `inscri` têm zero ocorrências no material oficial do TSE. O risco descrevia um prazo administrativo inexistente. Deixa de bloquear a S07 e a `pre-prod-checklist.md`. Efeito colateral corrigido: o `User-Agent` que declarava cadastro era falso ([ADR-0020](../architecture/adrs/0020-conformidade-res-23751-2026.md)). |
| ~~TSE muda formato EA20 sem aviso~~ → *"CONFIRMADO em 2026-05-17: TSE anunciou volta ao JSON para 2026 (não EA20). Audiência técnica início julho/2026. Spec 001 precisa refactor parser EA20 → JSON."* | **2026-09-05** | **Falso em duas camadas.** O **EA20 foi mantido** e sempre foi JSON — a doc de maio confundiu "mudança de leiaute" com "mudança de formato de serialização". O TSE nunca anunciou substituição: o EA20 segue listado como leiaute vigente no material de julho/2026. A audiência técnica **ocorreu** (julho/2026) e seu material é hoje a fonte primária. O que de fato mudou foi a **estrutura interna** do EA20, cujo diff campo-a-campo já foi feito e absorvido ([tse-2026-leiautes.md](./tse-2026-leiautes.md), commit `c6395a3`). Não há refactor "EA20 → JSON" pendente. O risco residual de mudança de campo migrou para a linha ativa **"Leiaute sem freeze"**. |
| ~~Data exata do simulado oficial TSE 2026 ainda não publicada~~ (Alto / Alta) | **2026-09-05** | **Datas publicadas**: **15–17/09/2026** (1ª janela) e **22–24/09/2026** (2ª janela), das **9h às 12h e das 14h às 17h BRT**. O calendário da S07 já foi re-baselinado sobre elas. Ressalva de proveniência: as datas vêm da pesquisa no site do TSE de 2026-09-05, não do slide da audiência técnica — este apenas diz que "a agenda de testes e demais informações serão divulgadas no site do TSE" (`apresentacao-interessados-2026.txt:237`). |
| ~~Resolução TSE 2026 publicada tardiamente ou com mudanças técnicas~~ (Alto / Média — "watch semanal no portal do TSE; advogado em standby") | **2026-09-05** | **Publicada**: Res. TSE nº 23.751/2026, divulgação de resultados no Título III, Cap. VI, arts. 264–269. O watch semanal por publicação encerra. As obrigações efetivas estão catalogadas em [regulatory.md § 4](./regulatory.md); os limites técnicos viraram as quatro linhas ativas no topo desta tabela. Errata ou nova resolução para o 2º turno reabrem o item. |
| ~~Fixture de teste renderizada em produção, exibindo resultado falso~~ (Crítico / Média — "home caía em fixture da fixture do modelo") | **2026-09-13** | **Risco residual mitigado em 13/09**: `app/(pres)/page.tsx` renderizava a fixture hardcoded do modelo **incondicionalmente**, ignorando `process.env.NODE_ENV` — resultado não verificado contra ambiente. Fora de `development`, a home agora renderiza o estado honesto de espera (RF-133, "Aguardando o primeiro boletim…") — ✅ **e desde 2026-09-19 o RF-133 EXISTE**: quando esta linha foi escrita, em 13/09, ela citava um ID que não estava em spec, matriz, índice nem código. Formalizado em [spec 019](../specs/019-fase-pre-eleicao/spec.md), com a asserção negativa que discrimina (a tela não pode exibir "0%" no lugar do número ausente). **Lição durável**: um fallback de conveniência que, em vez de degradar gracefully, fabrica dado com **cara de verdadeiro** é pior que nenhum fallback — a falsidade fica invisível. **Verificação de produção**: depois de qualquer deploy, testar se a home sob `NEXT_PUBLIC_MODEL_FALLBACK_TIER=remote` exibe "Aguardando", não resultado eleitoral. Procedure adicionado em runbook. |

## Cross-refs

- Regulamentação (Res. 23.751/2026, arts. confirmados, limites do CDN): [./regulatory.md](./regulatory.md)
- Leiautes TSE 2026 (fonte de verdade técnica; § 6 = orçamento de fan-out): [./tse-2026-leiautes.md](./tse-2026-leiautes.md)
- ADR-0020 (conformidade sem cadastro prévio): [../architecture/adrs/0020-conformidade-res-23751-2026.md](../architecture/adrs/0020-conformidade-res-23751-2026.md)
- Disponibilidade: [../nfr/availability.md](../nfr/availability.md)
- Modelo (penalização <5%): [../specs/002-modelo-estatistico/](../specs/002-modelo-estatistico/)
- Runbook: [../operations/runbook.md](../operations/runbook.md)
- Retrospective S03 (origem dos 6 itens novos abaixo da divisória): [../sprints/2026-S03-f3-modelo.md#retrospective](../sprints/2026-S03-f3-modelo.md#retrospective)
