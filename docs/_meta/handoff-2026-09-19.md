---
title: Handoff — 2026-09-19, os mapas ganham balão e a Câmara ganha hemiciclo
description: Estado, o que entrou, o que ficou aberto, as armadilhas novas e o prompt de retomada
status: stable
date: 2026-09-19
branch: main
supersedes: handoff-2026-09-18-fechamento-s07.md
---

# Handoff — 2026-09-19

~~**Nenhum commit empurrado.**~~ ⚠️ **Vencido ainda em 19/09 — o push foi feito; ver § 1.**
Árvore limpa. Confira o estado com `git status -sb`, **não** com número escrito aqui — um
handoff não consegue contar o próprio commit.

A sessão foi inteira de **UI, a pedido do dono**, e é um desvio consciente do objetivo
da sprint ativa (S08 — Enxergar, que é sobre alarme e monitoramento). O dono autorizou
explicitamente; está registrado.

---

## 1. 🔴 A primeira coisa a fazer

⚠️ **Atualizado ainda em 19/09: o push foi feito.** `origin/main` está em `62d2bc6`, o
mesmo commit que a `main` local — `git rev-list --count origin/main..main` devolve **0**.
Não há nada a empurrar.

**O CI ficou vermelho, e não é por causa do código.** A execução do push
([run `35460886370`](https://github.com/sudomenna/salacofre/actions/runs/35460886370))
tem `lint` ✅ e `typecheck` ✅; o job `test` roda `pytest` ✅ e **aborta no passo seguinte,
"Conferir o segredo do banco"**, em ~37 s. O passo é um guarda explícito
(`.github/workflows/ci.yml:152-162`) e a mensagem é dele:

> `DATABASE_URL_CI não está cadastrado nos segredos do repositório.`

O log da execução confirma a causa pelo próprio ambiente do passo: `DATABASE_URL:` sai
**vazio**, porque `secrets.DATABASE_URL_CI` não existe no repositório `sudomenna/salacofre`.
Sem ele, os 10 arquivos de teste que tocam o banco falhariam com `[db] DATABASE_URL ausente`
e a causa real ficaria escondida — o guarda existe justamente para falhar antes, com o
nome certo.

⚠️ **`gh secret list` não serve de prova aqui**: com o token desta máquina ele responde
**HTTP 403** (*"You must have repository read permissions or have the repository secrets
fine-grained permission"*), que é indistinguível de "lista vazia" para quem não olha o
código de saída. A evidência boa é o `DATABASE_URL:` vazio no log do passo.

**Quem resolve é o dono**, e só ele: cadastrar `DATABASE_URL_CI` nos segredos do
repositório apontando para um **branch descartável do banco no Neon** — nunca para
produção (o `DATABASE_URL` do `.env.local` é o banco que vai guardar a apuração de 04/10).

**Histórico das falhas na `main`** — as **4** execuções mais recentes falharam todas neste
mesmo passo (`35460886370` 19/09, `35327828559`, `35325782996` e `35325170894`, as três de
18/09). A 5ª mais recente (`35325072448`, 18/09 08:34) falhou por **outro** motivo — o passo
`Testes do modelo (pytest)` —, corrigido pelo commit seguinte
(*"fix(ci): o pytest do CI instalava o requirements errado"*). Ou seja: o CI está vermelho
desde 18/09, mas **não** foi sempre pela mesma causa.

---

## 2. O que entrou (4 commits)

| commit | o quê |
|---|---|
| `6b40f8b` | `/governador` e `/senador` ganham o coroplético nacional de presidente — e **cinco defeitos de produção** saem junto |
| `2c99a64` | ADR-0048 + propagação (spec 006 emendada, spec 016, traceability, 5 dívidas em `risks.md`) |
| `22a3a0d` | o balão do hover no formato NYT, com votos e parcial, estendido aos municípios |
| `16d4a26` | o hemiciclo de 513 cadeiras + o encaixe das bandeiras |

As mensagens de commit são longas de propósito e contam o porquê de cada decisão.
**Leia-as antes de mexer nessas áreas** — várias contêm argumentos que não estão em
mais lugar nenhum.

### O que mudou para o leitor

- Governador e senador têm mapa nacional colorido por partido, com hover e clique.
- O balão do hover mostra sete colunas (estado, % apurado, nome, partido, votos,
  parcial, projeção) e também aparece sobre municípios — lá **sem** projeção.
- A tela de deputado tem o hemiciclo com uma bolinha por cadeira.

---

## 3. 🔴 O que está aberto, em ordem de prioridade

⚠️ **A ordem desta seção mudou em 19/09.** O item 3.1 era "a mais importante" e **deixou de
ser**: a trava que ele pedia já existia desde 13/09 (ver abaixo). A prioridade real hoje é
**o resíduo (1) do 3.1** — o alarme sem destino, que é o objetivo da sprint ativa S08 —,
empatado com o segredo `DATABASE_URL_CI` do § 1. Os dois estão **travados no dono**, e não
há trabalho de código esperando por eles. Depois vêm 3.4 (documentação do hemiciclo), 3.3,
3.5 e 3.2 — este último também travado no dono.

### 3.1. A trava de sanidade do cargo 6 — ✅ já estava feita; sobraram dois resíduos

⚠️ **Esta seção afirmava algo factualmente falso, e a sessão de 19/09 confirmou que o
trabalho já estava feito.** O texto anterior dizia que o cargo 6 somava pares
(município, zona) **sem** `check_zona_merge_sanity`, citando `api/model/project.py:4171`,
`:3832` e `api/model/deputado.py:412` — **linhas que o crescimento dos arquivos já havia
invalidado**, e que por isso não podiam ser conferidas por quem lesse.

**A trava cobre os quatro cargos desde o commit `550fbb2`** (*"feat(model): a trava contra
multiplicação de votos passa a cobrir o cargo 6"*, 13/09 04:33 BRT), e ele **está na
`main`**: `git merge-base --is-ancestor 550fbb2 main` é verdadeiro — não ficou preso num
worktree, como aconteceu com os dois commits de 13/09 que só chegaram à `main` em 17/09.

Onde conferir, com as linhas de hoje:

- A chamada está em `api/model/project.py:5658`, dentro de `_do_project_proporcional`
  (`:5573`), precedida de um comentário de 12 linhas que explica o modo sem merge
  (`merged_rows=None`): soma `Σ e.te` dos pares de cada zona e compara com o eleitorado
  real dela, mesmo limiar `>= 1,8` do ramo majoritário, sem abortar o ciclo (§ 7).
- O modo tem docstring própria em `api/model/zona_merge.py:565-574`.
- Testes, rodados em 19/09 com `.venv-model/bin/python3.14 -m pytest`:
  `test_zona_merge_sanity.py` + `test_zona_merge_real.py` = **20 verdes** (o limiar);
  `test_deputado_payload.py -k guarda` = **3 verdes** e `test_orchestrator.py -k guarda`
  = **1 verde** — estes quatro são **teste de fio**: provam que a guarda é de fato
  *chamada* nos dois ramos, com o par "dispara quando a razão é 2,0" / "fica calada
  quando os pares trazem fatias".

⚠️ **A mesma afirmação vencida está na mensagem do commit `16d4a26`, seção "Dívida
registrada"** — e **não pode ser corrigida**, porque o commit já foi empurrado. Quem ler
aquela mensagem deve considerar este parágrafo o desmentido. A entrada de
`docs/reference/risks.md:28` foi reescrita como fechada na mesma passagem.

**O que sobrou no lugar — dois resíduos, e nenhum é a trava:**

1. **O alarme não tem destino.** A trava dispara `_log("error", …)` e `_alert_slack(…)`;
   `_alert_slack` (`api/model/project.py:5250`) lê `os.environ.get("SLACK_WEBHOOK_URL")` e,
   ausente a variável, loga `"slack alert skipped — SLACK_WEBHOOK_URL ausente"` e retorna —
   best-effort que nunca levanta (§ 7). Conferido em 19/09: a variável **não está** entre as
   27 chaves do `.env.local`. Em produção, uma multiplicação de votos de Deputado vira **uma
   linha de log na Vercel que ninguém lê na noite da apuração**. Isto é o objetivo declarado
   da sprint ativa **S08 — Enxergar**, e está travado no dono.
2. **A guarda vive dentro de um `if eleitorado:`** (`api/model/project.py:5657`). Logo acima,
   `fetch_eleitorado` tem um `except` que degrada para `eleitorado = {}` com um
   `_log("warn", "fetch_eleitorado falhou no ciclo proporcional", …)` — degradação
   **deliberada**, constituição § 7. Consequência que não estava registrada em lugar nenhum:
   **uma falha na consulta ao eleitorado desliga a trava em silêncio.** Registrado como
   observação, **não como defeito a consertar já** — o trade-off (não derrubar o ciclo) é
   legítimo. O que muda é a leitura do log: ausência de alarme de sanidade passa a ter duas
   causas possíveis, e o `warn` do `fetch_eleitorado` é o que as distingue.

**Sobre o hemiciclo**: a mensagem de `16d4a26` condiciona a estreia do widget à trava. Essa
condição **já estava satisfeita quando o commit foi escrito** — o que segue pendente para a
noite da apuração é o resíduo (1), o destino do alarme.

### 3.2. As 27 bandeiras — bloqueado no dono

O encaixe está pronto e testado. Falta o dono colocar os arquivos:

- **Onde**: `scripts/data/bandeiras-uf/`
- **Nomes**: `AC.svg` … `TO.svg` (sigla maiúscula)
- **Depois**: `pnpm gen:uf-flags`
- **Regras do gerador**: recusa `<script>`, `<image>`, `href` externo, `data:` raster;
  prefixa `id` interno com `ufflag-<SIGLA>-`; **falha o build** acima de 4 KB por
  bandeira ou 60 KB de sprite. Preencher `PROVENIENCIA.md` com licença e origem.

**Hoje são 0 de 27, e isso é o estado normal e testado**: `<UfFlag>` sem entrada devolve
`null`, a linha fica com nome e sigla. Não é defeito.

⚠️ O dono mandou o HTML do G1 achando que resolveria. Não resolve: o fonte traz o
**endereço** das imagens no CDN da Globo, não os arquivos. E copiá-los/apontar para lá é
usar ativo de terceiro na noite da apuração.

### 3.3. A fixture de "início de noite" — pergunta pendente ao dono

**Nenhuma fixture mostra a noite começando cinza** — que é exatamente o estado que o
dono escolheu e nunca viu. `simulacao/deputado.json` é 513/513 encerrado;
`edge-config/dep-current.json` é 463/513.

A pergunta que ficou sem resposta: criar a variante só para deputado quebra a invariante
escrita em `lib/dev/simulacao.ts:4-6` ("um estado COMPLETO e COERENTE — os quatro cargos,
as oito telas, o mesmo instante") — o dono veria deputado em 3% ao lado de presidente em
100%. Fazer para os quatro cargos é 7+ arquivos.

⚠️ Se for mexer: `lib/dev/simulacao.ts:21` documenta que a trava é **lista branca
explícita, nunca negação** (`FIXTURE_VARIANT ∈ {sim, sim-velho}` **e**
`NODE_ENV === "development"`). Ela existe porque o site publicou resultado inventado três
vezes. Siga a forma exata e estenda o teste de `varianteAtiva()`.

### 3.4. Documentação do hemiciclo

`docs/specs/017-deputado-federal/spec.md`: nenhum RF descreve hemiciclo, e o **corpo se
contradiz com o próprio frontmatter** (`:25-32` diz `implementing`, o frontmatter diz
`shipped`) — isso já estava assim antes da sessão. Precisa de emenda + ADR, no molde do
que foi feito para a spec 006 em `2c99a64`.

### 3.5. Dívidas menores, todas registradas

- `perf-budget.spec.ts`: o teto de 300 KiB do documento está **PROVISÓRIO** — o e2e não
  roda contra build local (o BotID derruba), então não houve calibração real. O caso
  imprime o número medido para a recalibração ser leitura.
- `/senador` e `/deputado-federal` no gate de a11y e2e: `/deputado-federal` já estava;
  `/senador` **não está** em nenhum dos dois gates (`risks.md`).
- `aria-describedby` do mapa: ausente em `/governador`, raso em `/senador`.
- Mapas de gov e sen **sem legenda nenhuma**.
- `ANALYZE=true pnpm build` **não funciona** — `@next/bundle-analyzer` não está
  instalado, mas está documentado em `CLAUDE.md § 12` e `docs/nfr/performance.md:94`.
  Pior que falhar: roda um build normal e quem executa acredita ter medido.

---

## 4. 🔴 As armadilhas que esta sessão descobriu

### 4.1. A fixture do `dev:sim` é MAIS RICA que produção

`data-pipeline/simulacao-gerar.ts` preenche `national.candidatos[].nome` em **todo**
cargo (180/180 medido). Produção não faz isso: `api/model/project.py:4885` grava
`"Candidato {id}"` fora do cargo 1 (RF-145), porque ali `id === 13` significa "o 13
nalguma UF".

**Consequência**: ver a tela certa no `dev:sim` **não prova** que o código está certo.
Aconteceu literalmente nesta sessão — abri a ficha do MT, li "WELLINGTON FAGUNDES (PL)"
e reportei ao dono que estava correto. Quem achou foi o `constitution-guard`.

**Regra**: verificação visual no `dev:sim` prova layout, cor e interação. Nunca
resolução de identidade, cargo, ou qualquer campo que o produtor preencha
condicionalmente. Para esses, o teste monta o payload no formato de produção.

### 4.2. Teste que entrega as props não vê o pai que não as passa

O balão municipal foi entregue com 11 testes verdes e **não aparecia na tela**: as props
existiam e ninguém as passava (`PersistentMapFrame` → `UfLeaderMapLazy` →
`ChoroplethMapUF`). Testes que entregam props direto provam que a peça funciona *se*
alimentada, nunca que alguém a alimenta.

Remédio adotado: `persistent-map-frame.municipioHoverProps.test.tsx`, que exercita a
composição e cai quando a passagem some.

### 4.3. Um gate que não existe é pior que um que falha

Os três orçamentos de RNF-007 contam `resourceType === "script"`. O hemiciclo é **zero
JS** — era invisível para todos eles e poderia inchar a página sem nenhum alarme.

Generalizando: ao acrescentar superfície de um tipo novo, **verifique se algum gate a
enxerga** antes de confiar que "passou".

### 4.4. Consertar um consumidor e esquecer o segundo

Aconteceu **duas vezes** na sessão, com a mesma classe de defeito:
identidade lida do bloco nacional em `buildHoverRows` **e** em `StateResultSheet`;
margem qualificada no seletor do mapa **e não** na ficha de estado.

**Regra**: ao corrigir leitura de um campo, procure ativamente os outros consumidores
**antes** de dar por fechado.

### 4.5. Correção a um relatório de subagente (registre a versão certa)

Um agente relatou que **três páginas** leem `FIXTURE_VARIANT` por fora de
`varianteAtiva()`, "meia simulação". **É uma só** — `app/(pres)/page.tsx:279` — e ela
compara `t2`/`pre` (as fixtures antigas de dev), **não** `sim`. As outras duas ocorrências
são comentário.

O docstring de `lib/dev/simulacao.ts:11-12` afirma que "nenhuma delas lê `FIXTURE_VARIANT`
por conta própria" — essa frase **deixou de ser literalmente verdadeira** por causa
daquela leitura antiga. A trava do modo simulado em si está intacta.

---

## 5. Baselines (medidos no fim da sessão)

| | valor |
|---|---|
| vitest | **2854** passando |
| ⚠️ vitest, arquivos | **10 falham na COLETA** sem `DATABASE_URL` — **isso É o baseline**, sai com exit 1 |
| pytest | **604** (`.venv-model/bin/python3.14 -m pytest`, nunca `pnpm test:py`) |
| lint | **484** arquivos, 0 erros |
| typecheck | limpo |
| chunk MapLibre | **285,3 KiB** de 300 (RNF-007b) — agora compartilhado por **três** famílias de rota |
| hemiciclo | 28.333 B com 513 cadeiras; `<main>` de `/deputado-federal` em 91.124 B |

---

## 6. Prompt de retomada

Copie daqui para baixo.

```
Retomando o SalaCofre em ~/Projetos/AtlasMenna.

Leia primeiro, nesta ordem:
1. docs/_meta/handoff-2026-09-19.md — este handoff, que supersede os anteriores
2. CLAUDE.md § 0 — o dono não é engenheiro; linguagem comum é o padrão
3. git log -4 --format=%B — as mensagens dos 4 commits da última sessão são longas
   de propósito e contêm argumentos que não estão em mais lugar nenhum

Confirme antes de agir:
- git status -sb (o push de 19/09 já foi feito; origin/main == main). O CI está
  vermelho por falta do segredo DATABASE_URL_CI no repositório, NÃO por causa do
  código — ver § 1 deste handoff antes de investigar.
- a sprint ativa (grep "^status: active" docs/sprints/*.md) é a S08 — Enxergar,
  cujo objetivo é alarme e monitoramento. A última sessão foi um desvio de UI
  autorizado pelo dono.

O que eu quero fazer agora é, em ordem de prioridade:

(a) ⚠️ ITEM VENCIDO — a trava de sanidade do cargo 6 JÁ EXISTE desde o commit
    550fbb2 (13/09), que está na main. Não reimplemente. Se você leu a mensagem
    do commit 16d4a26 (seção "Dívida registrada") e ela disser o contrário, ela
    está errada e não pode ser corrigida — ver § 3.1 deste handoff.
    O que sobrou é o ALARME SEM DESTINO: SLACK_WEBHOOK_URL não está configurada,
    então a trava grita para um log que ninguém lê. Isso é o objetivo da S08 e
    depende do dono colar o webhook.

(b) A documentação do hemiciclo: a spec 017 não tem RF que o descreva, e o corpo
    dela se contradiz com o próprio frontmatter. Emenda + ADR, no molde do que foi
    feito para a spec 006 no commit 2c99a64.

(c) Se eu tiver colocado os 27 SVGs em scripts/data/bandeiras-uf/, rode
    pnpm gen:uf-flags e verifique na tela.

Antes de qualquer implementação, me diga o que você conferiu e o que encontrou —
não comece a codar sem me mostrar o terreno.

Três regras desta base que já custaram caro:
- Ver funcionando no `pnpm dev:sim` NÃO prova que funciona em produção: a fixture
  de simulação preenche campos que produção deixa em placeholder.
- Aplique mutação de verdade em cada teste novo (editar, rodar, reverter). Mutação
  raciocinada não conta.
- NUNCA carregue .env.local para rodar teste: o DATABASE_URL de lá é produção.
```
