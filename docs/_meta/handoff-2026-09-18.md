---
title: Handoff — 2026-09-18, a spec 020 fecha e o trabalho vira quatro sprints sem data
description: Estado, decisões do dono e o plano de sprints para retomar em sessão nova
status: stable
date: 2026-09-18
branch: main
supersedes: handoff-2026-09-17.md
---

# Handoff — 2026-09-18

> ⚠️ **SUPERSEDED por [`handoff-2026-09-18-fechamento-s07.md`](./handoff-2026-09-18-fechamento-s07.md)**
> (2ª sessão do mesmo dia). A S07 **foi fechada** e a S08 está `active`; as instruções
> do § 2 ("onde retomar") deste arquivo já foram executadas. As **decisões do dono**
> (§ 3, D1–D12) continuam valendo e são a parte que não envelheceu.

**5 commits, todos empurrados e no ar** (`0e2966d` → `733fc9f`). Árvore limpa antes do
trabalho de planejamento; `main` sincronizada.

O dia teve duas metades. Na primeira, a **Fase 2 da spec 020** foi entregue e publicada —
a série por candidatura chega à tela nas 4 rotas — e duas decisões do dono viraram norma.
Na segunda, o checklist de prontidão para produção virou **quatro sprints sem data**, um
trilho externo e dois consertos de documentação.

---

## 1. O que entrou

### Spec 020 — Fase 2 completa e publicada

`fetch_series_por_candidato` (downsample no SQL, `DISTINCT ON` por balde de epoch,
último-do-balde nunca média), `anexar_ponto_corrente` (fecha a defasagem de um ciclo sem
mover a leitura para depois do INSERT), `ordenar_por_parcial` (porte de `rankByParcial`),
`montar_serie_por_candidato`, emissão nos dois escopos, e as **4 rotas com props reais nos
dois ramos de cada uma**.

🔴 **A rota de Senador NUNCA lia o Blob.** Ganhou `readUfDetail` em `Promise.all`,
espelhando as irmãs. Sem isso o RF-173 (as duas vagas) nunca renderizaria, porque a série
por UF mora só no Blob (ADR-0046 D3).

### Duas decisões do dono, implementadas e formalizadas em ADR-0047

**D1 — a linha do gráfico usa `textForParty`, não `colorForParty`.** Quatro cores de
partido reprovavam o piso de 3:1 do WCAG SC 1.4.11 sobre `--surface-page` (#f3f4f6):
**PSOL 2,08 · PSB 2,19 · outros 2,39 · NOVO 2,72**. **17 dos 31 partidos não mudam um
pixel** — onde a base já lia sobre o papel, a `-text` *é* a base.
⚠️ No tema **escuro** 8 tokens ficam mais pálidos sem que o contraste exigisse (UNIÃO
`#2c68c8`→`#d4dbff` é o mais visível). **Metade discricionária, reversível**, dito assim
no ADR.

**D2 — ciclo sem hora do TSE fica fora do eixo**, nos **dois** lados (leitura e ponto
corrente). ⚠️ A correção "óbvia" não funciona: `ts` é `defaultNow()`
(`lib/db/schema.ts:169`), e o `COALESCE(dado_ts, ts)` da leitura do ciclo seguinte
ressuscita o ponto no mesmo lugar errado — **a linha pisca**. Medido em produção: 22 de
25 ciclos sem hora; **13.180 linhas sem `dado_ts` na tabela inteira, ZERO com
`pct_atual`**.

### O plano de sprints

Quatro sprints **sem data**, ordenadas por dependência (decisão do dono), mais um trilho
externo para o que tem data de terceiros. Ver § 4.

---

## 2. Onde retomar

**A sprint `active` continua sendo a S07.** A primeira coisa a fazer é fechá-la e abrir a
S08.

1. **Auditar a S07 antes de fechar.** Ela tem **76 caixas abertas e 19 delas já estão
   feitas** — conferido no disco em 18/09. Fechar sem marcar envenena toda consulta futura
   de "o que falta". Triar as ~57 restantes: cada uma vai para uma das quatro sprints
   novas, para o trilho externo, para `backlog.md`, ou é descartada com a razão escrita.
2. **Abrir a S08** (`docs/sprints/2026-S08-f7-enxergar.md`) — `planned → active`.
3. **Primeira ação concreta da S08**: ligar `SLACK_WEBHOOK_URL`. É parada obrigatória do
   dono (é segredo) e destrava os 9 alarmes que hoje estão mudos.

⚠️ **As caixas mentem.** Antes de qualquer coisa, rode a onda 0 (`constitution-guard` +
`rf-coverage-checker`, ambos read-only, em paralelo) para remedir o disco. Foi a ausência
disso que deixou 19 caixas feitas e não marcadas, e a primeira caixa do
`pre-prod-checklist.md` resolvida em 05/09 e ainda vazia.

---

## 3. Decisões do dono — não reabrir

As de 17/09 continuam valendo (D1–D7 do handoff anterior). Novas em 18/09:

| # | Decisão |
|---|---|
| **D8** | 🔴 **A promessa de 95% de confiança será BAIXADA para o número real** (MAE@1h 2,3623pp, cobertura 82,5%) — **não** consertada. Duas tentativas falharam; uma terceira é cara-ou-coroa com dias em jogo. **Uma sessão zerada, sendo prestativa, vai querer tentar a terceira. Não tente.** |
| **D9** | **Sprints por dependência, sem data.** "Esqueça o calendário, vamos desenvolver conforme o meu ritmo" |
| **D10** | **Autonomia alta**: agentes seguem bloco a bloco sem confirmar cada tarefa. **Param apenas para: mudar a ESTRUTURA do banco (migration) ou DESPEJAR dados nele (import, seed, qualquer coisa com `ALLOW_DB_WRITE_TESTS`); publicar no site; gastar dinheiro; e decisão de produto que muda o que o leitor vê.** 🔴 **NÃO param pela escrita de rotina do ciclo** — o pipeline grava no banco a cada minuto, é o trabalho dele, e uma regra que não distingue rotina de intervenção ou trava tudo ou não trava nada. Afiada com o dono em 18/09. |
| **D11** | Os 3 itens com data do TSE saem das sprints → `docs/sprints/_trilho-externo.md` |
| **D12** | Enriquecimento de documentação = **só dois alvos**: consertar os furos que mordem, e o manual de orquestração paralela. **Não criar `CONTRIBUTING.md`, guia de teste nem guia de medição** |

> 🔴 **Achado de 18/09 que reforça a D8, e que o ADR da S10 precisa citar: o limiar de 2pp não tem casa formal.** `scripts/replay-2022.ts:4` e `:591` apresentam o gate como **RNF-006** — mas RNF-006 é *"Defasagem TSE → tela do usuário, &lt;90s"* (`docs/nfr/performance.md:17`), outra grandeza inteiramente. E **nenhum RNF do repositório menciona MAE** (`grep -rn MAE docs/nfr/` → vazio). Somado ao que `docs/reference/risks.md:74` já registrava — que os 2pp foram *"herdados de quando o gate era tautológico"* — o limiar não só nunca teve base empírica, como nunca teve requisito. Isso é o que separa "baixar a promessa" de "ajustar o gate para passar".

✅ **A regra do irreversível foi afiada pelo dono em 18/09** e já está na D10 acima.

---

## 4. O plano de sprints

| Sprint | `sequence` | Objetivo único |
|---|---|---|
| [S08 — Enxergar](../sprints/2026-S08-f7-enxergar.md) | 1 | Parar de trabalhar às cegas |
| [S09 — Provar](../sprints/2026-S09-f7-provar.md) | 2 | Ver o produto com dado, e medir o que a noite exige |
| [S10 — Só afirmar o que sustenta](../sprints/2026-S10-f7-verdade.md) | 3 | O site não promete nada que não cumpra |
| [S11 — Sobreviver ao pior](../sprints/2026-S11-f7-resiliencia.md) | 4 | Degradar com dignidade |

**A ordem é por dependência, não por urgência sentida**: toda verificação das sprints
seguintes depende de enxergar e ser avisado (S08); medir o pipeline só produz informação
depois disso (S09); a promessa real precisa do dado que a S09 produz (S10); e a S11 não é
pré-requisito de nada — **é onde cortar se apertar**.

**A convenção nova está escrita** em `docs/sprints/README.md` § "Duas formas de sprint":
sprint ancorada a data de terceiro mantém data (D1, D2, S12, S13); sprint ancorada ao
ritmo do dono usa `start: null` / `end: null` / `sequence:`. ⚠️ **Sprint sem data não é
sprint sem fim** — o que fecha é o `## Definition of Done`, verificável linha a linha.

⚠️ **Renumeração**: as pós-eleição eram S09/S10 e viraram **S12/S13**, para liberar a
faixa S08–S11. Cada uma carrega nota explicando.

### O trilho externo — o mais urgente do documento inteiro

`docs/sprints/_trilho-externo.md`. **O § 1 é executável agora e a janela é 22–24/09.**

✅ **O simulado JÁ ESTÁ ARMADO e dispara sozinho** — conferido na Vercel em 18/09. O preview
tem `TSE_BASE_URL=https://resultados-sim.tse.jus.br/simulado/simulado2026` (o endereço
CERTO), `TSE_COD_ELEICAO_FEDERAL=ele2026/21270`, `TSE_COD_ELEICAO_ESTADUAL=ele2026/21272`,
`CRON_ENABLED=true`, `INGEST_WINDOW=9-17`, `TSE_MAX_RPS=20`,
`TSE_TARGETS_WHITELIST=SP:1,SP:3,SP:5,SP:6`. **Ninguém precisa disparar nada.**

🔴 **O risco não é o simulado não rodar — é rodar e ninguém ver.** Não há canal de alarme
(`SLACK_WEBHOOK_URL` ausente nos dois ambientes), e a tabela "Registro das janelas" é
preenchida à mão. É por isso que a S08 (o alarme) vem antes de tudo: **ela é o que
transforma a janela de 22/09 em informação em vez de silêncio.**

ℹ️ A CLI da Vercel **está autenticada** (`sudomenna`) — anotações anteriores dizendo que o
login expirou estão velhas. Variáveis de ambiente podem ser lidas e configuradas por linha
de comando.

🔴 **A primeira janela (15–17/09) foi perdida quase inteira**: `git log` tem **zero commits
em 14, 15 e 16/09**. A vigia apontava para `resultados-sim.tse.jus.br/oficial`, que
responde **403 para sempre** — o segmento do caminho é o **ambiente**, e `oficial` é o de
produção. **403 ali nunca significou "ainda não publicado".**

---

## 5. Armadilhas de ambiente — custam horas se você não souber

1. **`pnpm test:py` está QUEBRADO** (usa o `python` do PATH). Use
   `.venv-model/bin/python3.14 -m pytest`.
2. 🔴 **NUNCA declare `ALLOW_DB_WRITE_TESTS` com o `.env.local` carregado.** O
   `DATABASE_URL` de lá é **produção**. Ponto único: `tests/integration/_guarda-banco.ts`.
3. **O gate e2e de a11y NÃO roda contra build local**, e a causa não se parece com o
   sintoma: o Vercel BotID lança fora da Vercel em `NODE_ENV=production`,
   `/api/projection` devolve 500 em laço, a rede nunca fica ociosa, e os testes morrem por
   **timeout de NAVEGAÇÃO**. O caminho que funciona:
   `PLAYWRIGHT_BASE_URL=https://salacofre.vercel.app npx playwright test tests/e2e/a11y-audit.spec.ts`
   — e exige `npx playwright install webkit`, senão 24 falham por browser ausente, **o que
   não é resultado de acessibilidade**. Está escrito no topo do arquivo de teste.
4. **Hook de pre-commit ativo** — `biome check` na árvore inteira, aborta o commit.
   Conserto: `pnpm lint:fix`. `--no-verify` exige ordem do dono.
5. **Rode a suíte INTEIRA antes de commitar.** Um bloco novo numa página entra no HTML que
   outras specs varrem.
6. **Ao desfazer mutação de teste, NUNCA `git checkout -- <arquivo>`** com trabalho de
   outro agente não commitado na árvore. Use cópia no scratchpad e prove com `diff`.

---

## 6. Gates e baseline — medidos em 18/09

| | |
|---|---|
| Vitest | **2.574 passando**, zero falhas (10 arquivos falham na **coleta** por falta de `DATABASE_URL` — pré-existente, de ambiente) |
| Pytest | **560 passando** |
| Lint / Typecheck | limpos, 434 arquivos |
| Build | verde, **sem** o aviso de tracing do Turbopack |
| a11y e2e | **48/48** contra o site publicado (6 rotas × 2 viewports × 2 temas × 2 navegadores) |
| Produção | `dpl_3a4847CdmebkC6ZfzupmgWNxpetx`, 9 rotas em 200 |

---

## 7. Lacunas conhecidas e NÃO fechadas

1. **Não existe RNF para contraste de NÃO-texto.** `docs/nfr/accessibility.md` só tem
   RNF-022 = contraste de **texto**, 4,5:1. O piso de 3:1 do SC 1.4.11 — critério inteiro
   da decisão D1 — não tem RNF, e a spec 020 citava o RNF-022 para uma garantia que ele
   não faz.
2. **Não há alarme para "série parada enquanto o placar anda".** O ponto sem hora é
   descartado em dois lugares, **os dois mudos**, sem contador. Se o parser de horário
   regredir, um ciclo **com voto** some da série em silêncio. É item da S08.
3. **O conserto de contraste cobriu só a linha do gráfico.** `_NationalChoroplethMapImpl.tsx:335`,
   `StateResultSheet.tsx:107`, `_candidateColor.ts:47` e as telas de Deputado seguem na
   cor-base. Caso diferente (área, não linha) e exige medição própria. É item da S10.
4. **`.claude/agents/spec-implementer.md` não declara `tools:`** — o escritor de escopo
   mais largo, em opus, herda tudo. Depois do episódio das 1.877 linhas de harness no banco
   de produção, vale considerar. **Não mexi**: restringir tira a delegação que o próprio
   texto dele manda usar — é decisão do dono.

---

## 8. Os erros desta sessão, e o que eles proíbem

🔴 **Três vezes em um dia um número dito com confiança não sobreviveu à medição.** Os três
estão registrados com data e método no ADR-0046 e no ADR-0047, porque o erro é a parte que
ensina:

1. **Estimativa vestida de garantia.** O ADR-0046 dizia "teto absoluto de 8.553 B por
   corrida, **para sempre**". Nunca fora medido.
2. **Amostra aleatória mentindo para baixo.** A correção mediu 8.775 B com valores passados
   por `round(x, 2)`, sem notar que `round(41.2, 2)` devolve `41.2` — quatro caracteres.
   **Ao medir payload, force o pior caso de cada campo.**
3. **Número repassado sem conferir.** Afirmei "no escuro a pior base é UNIÃO 3,53"; são
   3,09 contra o card e 3,36 contra a página. Peguei de um relatório e não medi.

**A conclusão que vale mais que os três**: não prometa constância onde ela não existe. O
tamanho do payload depende do nome de urna, que é dado do TSE — **nenhum número único podia
ser teto**. O teto que existe é em **pontos** (≤ 120).

**Duas correções vieram de subagentes para mim**, e é o comportamento que se quer: um
desconfiou do 8.775 e remediu; outro descobriu que `TSE_COD_ELEICAO` **ainda existe** como
fallback legado, depois de eu afirmar que não. Relatório de agente é hipótese — e o
orquestrador também é.

### Um conserto de segurança feito hoje

`.claude/agents/tse-parser-builder.md` afirmava que a resolução de 2026 estava "a ser
publicada" e que a Res. 23.736/2024 valia "como referência de práticas". O `CLAUDE.md` § 8
diz o **oposto**, com ênfase: foi a analogia com a 23.736 que produziu as premissas falsas
de maio/2026 (cadastro inexistente, abandono do EA20). **Era o único subagent com
`WebFetch`** — a crença errada morava no agente com maior chance de propagá-la. Corrigido.

### O que NÃO fazer

- Sondar URL adivinhada do TSE (404 bloqueia o IP por 10 min e queima janela de teste).
- Reabrir a decisão dos 95% (D8).
- Deixar duas sprints `active`.
- Criar `CONTRIBUTING.md` (D12).
- Encostar em `docs/PRD.md` (snapshot read-only).
- Publicar "82,5% de cobertura de um intervalo de 95%" — quase todo leitor lê errado, e
  ler errado aqui é o que a constituição § 1 existe para evitar. Publique **a faixa**.

---

## 9. Prompt de retomada

```
Retomando o SalaCofre. Leia primeiro `docs/_meta/handoff-2026-09-18.md` — ele tem o
estado, as decisões do dono e as armadilhas medidas.

O trabalho agora é por SPRINTS SEM DATA, ordenadas por dependência (decisão do dono).
A sprint ativa ainda é a S07 e a primeira tarefa é fechá-la:

  1. Auditar as 76 caixas abertas da S07 — 19 delas JÁ ESTÃO FEITAS e não foram
     marcadas. Confira no disco, não na caixa.
  2. Triar as restantes: cada uma vai para uma das 4 sprints novas, para o trilho
     externo, para o backlog, ou é descartada com a razão escrita.
  3. Abrir a S08 (`docs/sprints/2026-S08-f7-enxergar.md`), `planned → active`.

Leia também:
  - `docs/sprints/README.md` § "Duas formas de sprint" — a convenção nova
  - `docs/_meta/orquestracao-paralela.md` — quais agentes colidem entre si
  - `docs/sprints/_trilho-externo.md` — ⚠️ o § 1 é executável AGORA (janela 22–24/09)

Quatro coisas que custam horas se você não souber:
  1. `pnpm test:py` está QUEBRADO. Use `.venv-model/bin/python3.14 -m pytest`.
  2. NUNCA declare `ALLOW_DB_WRITE_TESTS` com o `.env.local` carregado — é produção.
  3. O gate de a11y NÃO roda contra build local (o Vercel BotID derruba, e o sintoma
     é timeout de navegação, que não se parece com a causa). Rode contra o site
     publicado com PLAYWRIGHT_BASE_URL, e instale o webkit antes.
  4. Hook de pre-commit ativo — `biome check` na árvore inteira aborta o commit.

Regra de teste desta casa: teste verde não prova nada. Para cada teste, aplique a
mutação você mesmo, confirme o vermelho, restaure e prove com `diff`.

E antes de acreditar em qualquer caixa marcada ou número escrito em documento:
remeça. Em 18/09, três números ditos com confiança não sobreviveram à medição.

Fale comigo em linguagem comum: não sou engenheiro.
```
