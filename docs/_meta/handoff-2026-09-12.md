---
title: Handoff — 2026-09-12, o cálculo de cadeiras validado contra 2022
description: Estado, pendências e próximos passos para retomar em sessão nova
status: stable
date: 2026-09-12
branch: main
supersedes: handoff-2026-09-11-noite.md
---

# Handoff — 2026-09-12

## Estado em uma frase

O cálculo de cadeiras do Deputado Federal reproduz **511 das 513** cadeiras de
2022, candidato por candidato — **a régua de desistência de 19/09 não será
acionada por causa do método**. Falta só payload e telas.

**Faltam 3 dias para o simulado 1 (15–17/09) e 22 para o 1º turno (04/10).**

## Ordem de leitura para quem chega agora

1. **Este arquivo.**
2. [`handoff-2026-09-11-noite.md`](./handoff-2026-09-11-noite.md) — os cinco
   defeitos silenciosos fechados na véspera, incluindo o que impedia o site de
   publicar qualquer coisa. **Leia as "Lições" e "A prosa que ficou para trás".**
3. [`../architecture/adrs/0027-conversao-votos-em-cadeiras-deputado-federal.md`](../architecture/adrs/0027-conversao-votos-em-cadeiras-deputado-federal.md)
   — o método de cadeiras, com o texto legal vigente e a jurisprudência.
4. [`../specs/017-deputado-federal/spec.md`](../specs/017-deputado-federal/spec.md)
   — o que falta está na tabela do `## Status`.
5. [`../testing/tse-simulados.md`](../testing/tse-simulados.md) — **o Passo 0 é a
   coisa mais importante do dia 15**, e virou um comando.

## Baselines (medidos ao fim desta sessão)

```
1.629 vitest (1 pulado) · 277 pytest · typecheck limpo
lint: 5 erros + 5 avisos — TODOS pré-existentes, não tente consertá-los
cross-refs de docs/: nenhuma quebrada
gate OT-4: segue reprovando (MAE@1h PT 2,3623pp / cobertura 82,5%) — bloqueia a spec 002, não as 016/017
```

Tudo commitado na `main`. **Dois commits não empurrados** (`e896114`, `562a4ab`)
— ver "Como empurrar" no fim.

---

## O que foi construído

### Deputado Federal (spec 017) — o cálculo está pronto e provado

| camada | onde | estado |
|---|---|---|
| Método de cadeiras (ADR-0027) | `api/model/cadeiras.py` | ✅ 21 casos de borda |
| Ponte EA20 → cadeiras | `api/model/deputado.py` | ✅ 28 testes, 2 verificados por mutação |
| **Golden de 2022** | `tests/unit/model/test_cadeiras_golden_2022.py` | ✅ **511/513** |
| Conferência ao vivo contra o TSE | `deputado.py::conferir_contra_tse` | ✅ |
| Ingestão (27 alvos UF, cron 15 min) | `vercel.ts`, `lib/tse/targets.ts` | ✅ desde 11/09 |
| Payload (`EdgePayload*` de Deputado) | — | ❌ **não existe** |
| Blob `deputado/uf/<SIGLA>.json` | `lib/blob/paths.ts` tem só o construtor de caminho | ❌ sem escritor nem leitor |
| Telas `/deputado-federal` e `/uf/[sigla]/deputado-federal` | — | ❌ **não existem** |
| Aba no `CargoTabs` | `components/layout/CargoTabs.tsx` | ❌ `disabled: true`, sem `href` |

**Três decisões do extrator que não são óbvias** (todas travadas em teste):

- **`fed[]` não é percorrida.** No EA20, `fed[]` é dicionário de composição, sem
  `par[]` nem `cand[]` (árvore oficial em
  `tse_docs/txt/tse-ea20-arquivo-de-resultado-unificado.txt:417-434`). Todo voto
  vem por `agr[]`, e a federação é `agr[].tp == "f"` — já com os partidos dentro.
- **`sqcand`, nunca `cand.n`.** O número de urna se repete entre UFs e partidos
  no proporcional; usá-lo como identidade funde candidatos distintos.
- **`lugares_a_preencher` sai de `carg[].nv`** (RF-124), nunca de tabela embutida.

**Três campos do TSE que ninguém lia** e que agora são a rede de segurança em
tempo real: `carg[].nv` (vagas), `carg[].qe` (o quociente **dele**) e `agr[].vag`
(as cadeiras que **ele** atribuiu). `conferir_contra_tse` compara os três.
⚠️ **Só conclusivo com `tf == "s"`** — na apuração parcial o TSE recalcula a cada
boletim e divergir é esperado.

### Senador (spec 016) — entregue

Rotas `/senador` e `/uf/[sigla]/senador`, aba habilitada, gate de a11y **PASS**
(0 violações em 16 combinações; RNF-024/teclado verificado). Ingestão **por
zona** desde 11/09 (emenda (b) do ADR-0026). Falta só mapa municipal e "maiores
colégios", deliberadamente fora do escopo pré-simulado.

---

## O golden de 2022 — o que ele prova e o que não prova

**Prova**: 511/513 cadeiras, e **fase 1 exata nas 27 UFs** (quociente eleitoral +
quociente partidário + cláusula dos 10% do art. 108).

**Procedência**: o gabarito é a coluna `DS_SIT_TOT_TURNO` do dataset aberto do
TSE — desfecho oficial por candidato, não reconstrução nossa. Os CSVs têm
`DT_GERACAO = 11/09/2026`, então **já refletem** o recálculo posterior à ADI
7228 (os embargos de 13/03/2025 derrubaram a modulação e a decisão retroagiu a
2022).

**NÃO prova o arredondamento do art. 106.** Quatro UFs caem na fração exata de
0,5 (AP, MT, SC, TO), então a regra **é exercitada** — mas 1 voto de diferença no
quociente não move cadeira nelas. Verificado por mutação: trocar `> 0,5` por
`>= 0,5` deixa os 7 testes do golden passando. Quem protege a regra são os casos
sintéticos de `test_cadeiras.py::test_caso1_*`.

**Duas divergências, nomeadas e não explicadas**: MG (NELY AQUINO, PODE) e RS
(BIBO NUNES, PL). Nos dois o TSE deu a vaga à agremiação de **menor** média,
tendo a de maior média candidato acima do piso de 20%. Descartado pelo dado
(quociente conferido por duas fontes, votos idênticos entre datasets, ordem não
muda com ou sem legenda). Hipótese não confirmada: decisão judicial posterior.

### A armadilha que este golden pagou

A primeira medição deu **505/513**, com exatamente uma cadeira errada em 8 UFs —
sistemático demais para acaso. Era defeito **do gerador**: os votos de legenda
vinham do arquivo `_BR`, e **o cargo 6 não está lá**, só nos arquivos por UF.
Entravam todos como zero. Corrigido, seis cadeiras se acertaram sozinhas.

> Um teste de referência pode estar medindo com defeito e ainda parecer bom.
> 98% é exatamente a faixa em que isso passa despercebido.

---

## Próximos passos, na ordem

### 1. Payload de Deputado (`lib/edge-config/types.ts`)
Não existe nenhum tipo de agremiação, cadeira, legenda ou quociente. O mais
próximo de reusar é `EdgeComposicaoVagas` (`types.ts:570-589`), mas ele é
agregação de majoritário, não distribuição por quociente. Precisa de:
agremiação (com composição, votos nominais, legenda, totais), quociente eleitoral
e partidário, `cadeiras`/`vagas_obtidas` por agremiação, eleitos/suplentes,
`empates_indeterminados`, `vagas_nao_preenchidas`, e o tipo do objeto Blob.

⚠️ **`cadeiras` vai à tela; `vagas_obtidas` NUNCA** (ADR-0027, caso de borda 2b).
A segunda conta o quociente partidário inteiro ainda que não preenchido, e numa
UF de 10 vagas exibiria 11.

### 2. Ligar o modelo (`api/model/project.py`)
Hoje `cargo=6` **roda e produz lixo no nacional**: `compute_national` não tem
branch por cargo e agrega por `cand.n` (número de urna), que **colide entre
UFs**. As linhas por UF ficam corretas; a linha nacional é inválida. Nenhuma
chamada a `cadeiras.py` existe em `_do_project`.

### 3. Read path do Blob
`deputadoUfBlobPathname` (`lib/blob/paths.ts:168-171`) devolve
`deputado/uf/<SIGLA>.json` e **não tem caller**. O molde do leitor é
`lib/blob/uf-detail.ts::readUfDetail` — resultado discriminado, nunca lança,
nunca devolve `null` cru.

### 4. Telas + aba
`/deputado-federal` e `/uf/[sigla]/deputado-federal`; habilitar a aba `"dep"` em
`CargoTabs.tsx`. RF-127 (incerteza explícita), RF-128 (cadência de 15 min),
RF-130 (voto de legenda distinguível do nominal).

### 5. Gates
`rf-coverage-checker`, `constitution-guard`, `a11y-perf-auditor`, `spec-syncer`.

---

## Pendências do usuário

| tarefa | prazo | estado |
|---|---|---|
| **Chamado no TSE** (`30308800.tse.jus.br`, descrição começando com `Resultados - Divulgação`) | **venceu 12/09** | 🔴 `pnpm tse:watch --once` em 11/09 às 06:49: `changed=false`, `ele-c.json` ainda em `ele2024` |
| **`EDGE_CONFIG_TOKEN`** (escopo do time `team_AqxGDYz4Zxs5wUBUDzIpcwBm`; pessoal recebe 403) | antes de 15/09 | 🔴 destrava **só** a gravação no Global Config |

---

## Armadilhas desta base — leia antes de codar

1. **Relatório de subagent é hipótese.** Nas duas sessões de 11–12/09, **nove**
   erraram de formas plausíveis. Confira no disco, inclusive o que o handoff diz.
2. **Teste que passa não prova nada; teste que reprova, sim.** Seis testes meus
   nestas sessões passavam sem discriminar. **Verifique por mutação.**
3. **A prosa fica para trás quando o dado muda.** Quatro frases na tela viraram
   falsas em 11/09 ao mudar a granularidade do Senador. Contramedida adotada:
   asserção **negativa** (o teste proíbe a frase errada) e valores derivados da
   tabela canônica, nunca literais na página.
4. **`Map.set` sobre linhas de par** é o modo de falha padrão desde a migration
   0006. Toda leitura por `(uf, cod_zona)` precisa de `SUM(...) GROUP BY`.
5. **Não rode `constitution-guard` em paralelo com agente de implementação** — ele
   faz `git stash` e o working tree some por alguns minutos.
6. **`*/5` dentro de comentário JSDoc fecha o bloco** e o formatador destrói o
   arquivo. Escreva cadência de cron em prosa.
7. **Derrube o que você subir.** O mock do TSE ficou 1h06 de pé esquecido.
8. **Outras sessões do Claude podem estar na mesma pasta.** Confira antes.

---

## Disco

**16 GB livres.** Em `build/` (git-ignored) há **8,3 GB** de dataset do TSE, dos
quais 8,1 GB são o `votacao_candidato_munzona_2022`. Mais 552 MB do zip solto em
`tse_docs/`.

**Pode apagar tudo com segurança**: o golden commitado tem 232 KB e não depende
dos CSVs — eles só servem para **regerar** a fixture, e
`scripts/build-cadeiras-golden.py` documenta de onde baixar de novo.

⚠️ **O CDN do TSE responde 403 a cliente automatizado**, inclusive com o
User-Agent do projeto e com `GET` parcial. O download é manual, pelo portal
https://dadosabertos.tse.jus.br/dataset/resultados-2022.

Também solto e **não é do projeto** (decisão do usuário pendente):
`tse_docs/Switchcraft.pdf`, `tse_docs/painel-exportar.zip`, `tse_docs/painel-exportar/`.

---

## Não reabrir

ADR-0035 D1/D2/D3; as duas emendas ao ADR-0026 (cron por segmento de rota;
Senador em zona); o teto de rps por cargo (25 para os três pesados, 5 para
Deputado — pior caso agregado 80); D21/D22/D23 do ADR-0034 e as quatro exceções
constitucionais; a degradação pré-acordada da 017 (que **não** será acionada pelo
método).

---

## Comandos

```bash
set -a; . ./.env.local; set +a            # obrigatório antes de tudo que toque o banco
pnpm typecheck && pnpm lint && pnpm test  # 1.629 verdes
.venv-model/bin/python3.14 -m pytest -q   # 277 verdes
pnpm verify-fatia-premise --fixtures tests/fixtures/tse/2026-sim   # Passo 0 do dia 15
pnpm list-targets --env production --cargo 6                       # 27; exige TSE_COD_ELEICAO
.venv-model/bin/python3.14 scripts/build-cadeiras-golden.py         # regera o golden (precisa dos CSVs)
pnpm tse:watch --once                     # exit 0 = TSE sem mudança, 2 = mudou
```

### Como empurrar

O `gh` tem **duas** contas nesta máquina; a ativa é `cneeducacao`, mas o
repositório é `sudomenna/salacofre`. Push com a conta errada dá 403.

```bash
gh auth switch --user sudomenna && git push origin main && gh auth switch --user cneeducacao
```

**Devolva a conta** — há outro projeto na máquina que depende dela.
