---
title: Handoff — 2026-09-13, a spec 017 fechada e a descoberta de que nada estava no ar
description: Estado, pendências e dívidas para retomar em sessão nova
status: stable
date: 2026-09-13
branch: main
supersedes: handoff-2026-09-12.md
---

# Handoff — 2026-09-13

> ## ⏱️ Atualização das 05h — leia antes do resto deste arquivo
>
> **Este documento foi escrito às 00h59 e o trabalho continuou até as 05h.** O que vem abaixo
> dele descreve corretamente o estado daquela hora; três afirmações centrais **caducaram**:
>
> | o arquivo diz | hoje |
> |---|---|
> | spec 017 em `implementing`, RF-127 pela metade | **`shipped`**, RF-127 **completo** — o intervalo de cadeiras existe |
> | Deputado: 27 alvos por UF, cron de 15 min | **~6.110 alvos de par município×zona**, 6 fatias, volta de **30 min** ([ADR-0036](../architecture/adrs/0036-deputado-federal-granularidade-zona-fatiada.md)) |
> | baseline 1.708 vitest / 333 pytest | **1.750 vitest / 374 pytest** |
>
> **19 commits, todos empurrados e no ar** (`c7f6a04` → `45b79e1`, build confirmado por
> `source: git` na API). Dois deles vieram de outra sessão trabalhando em paralelo.
>
> **ADRs novos**: [0036](../architecture/adrs/0036-deputado-federal-granularidade-zona-fatiada.md)
> (granularidade fatiada) e
> [0037](../architecture/adrs/0037-uf-sem-faixa-entra-como-constante-no-nacional.md) (UF sem faixa
> na soma nacional).
>
> **Cinco defeitos silenciosos achados e corrigidos** — o padrão vale mais que os casos, e é o
> mesmo nos cinco: *a verificação confirmava a forma e não o conteúdo.*
> 1. O interruptor de emergência **congelava o dado** em vez de reverter.
> 2. A whitelist do preview **rejeitava os cargos 5 e 6 no código** — o simulado rodaria verde
>    sem tocar em Senador nem Deputado.
> 3. Uma escotilha de diagnóstico esquecida ligada **desativava o interruptor**.
> 4. A tela **afirmava ao leitor** uma granularidade desfeita três horas antes (e o conserto
>    reintroduziu uma variante: eram **três** estados, não dois).
> 5. A trava contra multiplicação de votos **não estava ligada ao ciclo** do cargo 6 — e o
>    ADR-0036 afirmava por escrito que ela vinha junto.
>
> ⚠️ **Trabalho de outra sessão ainda NÃO commitado**: `dado_ts` (separar a hora do dado da hora
> do cálculo) + alarme de apuração parada + ADR-0038. Aguardava decisão do dono do projeto às
> 05h. **Confira o git antes de assumir que entrou ou que não entrou.**

## Estado em duas frases

A **spec 017 (Deputado Federal)** foi implementada ponta a ponta e passou nos 4 gates. E
descobrimos, no meio do caminho, que **nada do que este projeto construiu em quatro meses estava
no ar** — a produção servia o build de 17/05, e ninguém tinha como saber, porque todos os
indicadores diziam que estava tudo certo.

**Faltam 2 dias para o simulado 1 (15–17/09) e 21 para o 1º turno (04/10).**

## Baselines (medidos duas vezes ao fim desta sessão, mesmo resultado)

> ⚠️ **Números de 00h59. Ao fim da madrugada: 1.750 vitest · 374 pytest.** O resto do bloco
> (lint, gate OT-4, golden de cadeiras) continua valendo.

```
1.708 vitest (123 arquivos, 1 pulado) · 333 pytest (.venv-model/bin/python3.14)
typecheck limpo
lint: 5 erros + 5 avisos — TODOS pré-existentes, não tente consertá-los
cross-refs de docs/: nenhuma quebrada
gate OT-4: segue reprovando (MAE@1h PT 2,3623pp / cobertura 82,5%) — bloqueia a spec 002, não a 017
golden de cadeiras 2022: 511/513, fase 1 exata nas 27 UFs
```

⚠️ **`pnpm test` exige `set -a; . ./.env.local; set +a` antes.** Sem isso, 10 arquivos de banco
falham por falta de string de conexão e parece regressão — não é.

⚠️ **Os testes de integração sobem Python de verdade** (~11 s cada, teto de 60–100 s). Sob
disputa de CPU eles passam de 12 s e **um deles reprova de forma intermitente** — medido em 12/09
enquanto um agente rodava pytest em paralelo. Não é defeito do produto; é a máquina competindo
consigo mesma. Se ver uma falha isolada nesses arquivos, rode de novo com a máquina ociosa antes
de investigar.

## Ordem de leitura para quem chega agora

1. **Este arquivo.**
2. [`../reference/risks.md`](../reference/risks.md) — as **quatro primeiras linhas** de *Riscos
   ativos* são de 12–13/09 e reenquadram o projeto inteiro.
3. [`../operations/runbook.md`](../operations/runbook.md) — a **rota de fuga de deploy** está no
   topo, de propósito.
4. [`../specs/017-deputado-federal/design.md`](../specs/017-deputado-federal/design.md) — o
   contrato do payload de Deputado (D1–D10). É o documento a ler antes de tocar nesse cargo.
5. [`../testing/tse-simulados.md`](../testing/tse-simulados.md) — **Passo 0 e Passo 0b** são o que
   importa no dia 15.

---

## Parte 1 — O que foi construído

### Spec 017 (Deputado Federal): ~~`implementing`~~ → **`shipped` em 13/09, 05h**

| camada | onde | estado |
|---|---|---|
| Método de cadeiras (ADR-0027) | `api/model/cadeiras.py` | ✅ 21 casos de borda |
| Ponte EA20 → cadeiras | `api/model/deputado.py` | ✅ |
| Golden de 2022 | `tests/unit/model/test_cadeiras_golden_2022.py` | ✅ **511/513** |
| **Payload nacional e por UF** | `api/model/deputado_payload.py` | ✅ novo |
| **Escrita e leitura do Blob** | `lib/blob/deputado-uf.ts`, `writer.ts` | ✅ novo |
| **Telas** `/deputado-federal` e `/uf/[sigla]/deputado-federal` | `app/(dep)/` | ✅ novas |
| **Aba `dep`** | `components/layout/CargoTabs.tsx` | ✅ habilitada |

**Gates**: `rf-coverage-checker` PASS (12 RFs, 2 parciais por escopo) · `constitution-guard` PASS
(0 violações) · `a11y-perf-auditor` PASS (16/16 combinações axe-core, bundle abaixo do piso de
framework) · `spec-syncer` executado.

~~**Não é `shipped`** porque RF-127 saiu pela metade~~ — **entrou em `2bcee57`**, e a spec foi promovida em `b1fbeca` com os 4 gates aprovados. A Parte 3 abaixo descreve o estado anterior; ver a atualização das 05h no topo.

### Três decisões que não se reabrem (design.md)

- **D1** — Deputado **não** reusa `EdgePayload`. `EdgeNational` é inteiramente majoritário
  (`candidato_a_id`, `needle_position`, `p_segundo_turno_overall`, `cenarios_2t`) e numa corrida
  proporcional nenhum desses campos tem referente. `readProjection` agora recusa `"dep"` **no
  tipo** (`CargoMajoritario = Exclude<Cargo, "dep">`), não por convenção.
- **D2** — `vagas_obtidas` **não atravessa a fronteira** (RF-125.1). Conta o quociente partidário
  inteiro ainda que não preenchido; numa UF de 10 vagas exibiria 11. Travado por **asserção
  negativa**: o teste procura a string no JSON serializado e falha se a encontrar.
- **D9** — o número exibido é voto **apurado**, não projetado, e **a tela é proibida de chamá-lo
  de projeção**. Ver Parte 3.

---

## Parte 2 — Os defeitos que estavam armados

Todos pré-existentes, todos silenciosos, todos corrigidos em 12–13/09.

### 1. O gravador mandava o Senador para a chave do Presidente

`cargoFromTseNumeric` era `cargoTse === 3 ? "gov" : "pres"`, escrito quando a eleição tinha dois
cargos. Com Senador (5) e Deputado (6) na tabela desde 11/09, **todo payload de cargo 5 ia para
`projection-current-pres-t1`** — chave válida, JSON válido, eleição errada, nenhum erro.

O outro lado: `/senador` lê `projection-current-sen-t1`, chave que nunca era escrita. A tela de
Senador, entregue em 11/09 com gate de a11y PASS, **nunca teria dado em produção**.

**Estava armado para disparar quando o usuário colasse o `EDGE_CONFIG_TOKEN`** — sem credencial,
`writeEdgePayload` é no-op. Corrigido antes de o token existir.

### 2. A função de alarme se derrubava sozinha

`_alert_slack` chamava `_log(level, msg, msg=...)` — `TypeError` — sempre que `SLACK_WEBHOOK_URL`
está ausente. A função que promete nunca levantar levantava, em todo ambiente sem Slack,
**exatamente no momento em que um alerta disparava**, derrubando o ciclo junto.

### 3. As telas chamavam de projeção o que não é

As duas telas de Deputado diziam "Projeção Atlas Menna" e montavam `<ForecastTransparency>`, que
calcula `pctModel = 100 − pctApurado`: com 71,4% apurado ele imprimia **"Modelo 28,6%"**,
atribuindo 28,6% da bancada a um modelo que não existe. Substituído por
`components/blocks/DeputadoMetodologia.tsx`, sem tocar o componente compartilhado (serve 3 rotas).

### 4. A produção estava em 17/05 — e por dois motivos independentes

**Causa A: uma rolling release parada há 4 meses.** Configuração 10% → 50% → 100%, os dois
primeiros com **aprovação manual**. Uma release começou em **17/05 06:58**, parou nos 10%
esperando um clique que nunca veio, e **toda publicação posterior entrou em fila**.

**Causa B: o GitHub App da Vercel não estava instalado.** `github.com/settings/installations`
dizia "No installed GitHub Apps". A autorização OAuth continuava lá — por isso login e painel
funcionavam. **Identificar o usuário e ler o repositório são permissões diferentes.**

⚠️ **Instalar o app não bastou.** São duas camadas: o app instalado (acesso ao repositório) e o
webhook do projeto (avisa do push). Depois de instalar, a Vercel já enxergava o repositório e um
push **não disparou build por 18 minutos**. `vercel git connect` respondeu *"already connected"* e
não fez nada. Só `vercel git disconnect` **seguido de** `connect` refez o webhook.

---

## Parte 3 — O que a tela de Deputado NÃO faz, e diz que não faz

**Não é projeção.** O número é a distribuição de cadeiras do ADR-0027 sobre os votos **já
apurados** — "como ficaria a bancada se a contagem parasse agora".

Para projetar, falta o **bootstrap de voto por agremiação**, que não existe para o cargo 6
(`extrapolation.py` resampleia candidato-por-zona, para majoritário). É a mesma peça que falta
para o intervalo de RF-127.

**O custo foi medido e cabe** (`scripts/bench-deputado.py`): ciclo das 27 UFs em **20,5 ms**;
redistribuição de cadeiras sobre 1.000 resamples × 27 UFs em **11,1 s**, contra teto de 60 s. O
obstáculo nunca foi CPU — é modelagem.

Até lá, o que sai de RF-127 é a **marcação**: cadeira ganha em rodada de sobra com margem apertada
vai como `indefinido`, e a tela diz isso em texto.

---

## Parte 4 — Infraestrutura, agora funcionando

| | estado |
|---|---|
| Site público (`salacofre.vercel.app`) | ✅ código de 13/09 |
| `git push` → build automático | ✅ **provado** (`source: git`, não `cli`) |
| Rolling releases | ✅ **desligadas** (`rollingRelease: null`) |
| Deploy hook de emergência | ✅ criado **e testado** — ver runbook |
| Gravação no Global Config | ✅ **primeira gravação real do projeto** |
| Preview armado para o simulado | ✅ 7 variáveis, falta só `TSE_COD_ELEICAO` |
| Vigia automática do TSE | ✅ de hora em hora (`~/.claude/scheduled-tasks/vigia-tse-2026`) |

### O que a primeira gravação real revelou

`pnpm edge-config:smoke` passou nos 4 passos obrigatórios e respondeu três perguntas que eram
suposição desde que o projeto existe:

1. **O separador `:` do ADR-0012 nunca teria funcionado.** A API devolve `400 validation_error`.
   A troca para `-` não foi conservadorismo — era a única forma correta.
2. **O store cabe**: 106 B de 1.000.000 B (0,01%).
3. **`ecfg_mcoa3usgvm5dbqb27vae8ptmpdxl`, documentado desde 11/09, está correto.**

⚠️ **Três armadilhas de credencial**, para quem repetir: o token precisa ter escopo de **time**,
não de projeto (o de projeto lê `/v9/projects` mas recebe 403 em recurso de time, e o upsert volta
`404 Edge Config Item not found`, que parece store inexistente e não é); falta uma terceira
variável não documentada em lugar nenhum, `VERCEL_TEAM_ID`; e a CLI `vercel` desta máquina está
logada em **outra conta** (Bruna Puga) — o caminho que funciona é `--token` ou `curl` na API.

---

## Parte 5 — Pendências, com dono

### Do usuário

| tarefa | prazo | estado |
|---|---|---|
| **Chamado no TSE** | aberto em 13/09 | 🟡 aguardando resposta. Vigia automática avisa. ⚠️ Se nada até a manhã de **15/09**, o protocolo **para no Passo 1** — não prosseguir por tentativa e erro |
| **Decidir sobre `SLACK_WEBHOOK_URL`** | antes de 15/09 | 🔴 sem ela **não há alarme nenhum** na noite da apuração |
| ~~`EDGE_CONFIG_TOKEN`~~ · ~~variáveis de produção~~ · ~~rolling releases~~ · ~~integração Git~~ | — | ✅ feitos em 12–13/09 |

### Técnicas

- **RF-127 completo** — o bootstrap de voto por agremiação. Custo medido e dentro do teto.
- **Contraste de cor de partido (WCAG 1.4.11)** — PSOL **2,08** e NOVO **2,72** contra piso de
  **3:1**, em modo claro, como preenchimento de área. É do **gerador da paleta**
  (`scripts/gen-party-scale.ts`), alcança o mapa nacional e a ficha de estado. Nenhum gate pegava:
  os testes medem `-chip`/`-ink` e `-text`/papel, nunca o token base; e o axe-core só olha texto.
  **Prazo: antes de 04/10.**
- **`GET /api/projection?cargo=deputado-federal`** — no `apis:` da spec, não implementado. Mesmo
  estado do Senador.
- **e2e/a11y das 2 rotas novas** em `tests/e2e/a11y-audit.spec.ts`.
- **Pós-outubro**: trocar o token da Vercel por um de prazo curto e tirá-lo do texto puro do
  `.env.local`.

---

## Parte 6 — O que segue em aberto e só o simulado resolve

1. **A premissa da fatia por município** (Passo 0). Se cair,
   `merge_pairs_into_zonas` **multiplica votos por até 8×** em ~62% das zonas. Quatro linhas de
   evidência convergem a favor, nenhuma é prova. `pnpm verify-fatia-premise` decide — exit 0 =
   fatia, 2 = multiplicação (**pare**), 1 = inconclusivo (**não** é sinal verde).
2. **`carg[].nv` a 0% apurado** (Passo 0b, novo). Os únicos registros com o campo no banco são do
   **nosso próprio mock** (`COLIGACAO SINTETICA`, siglas `S11`). Não há snapshot de cargo 6 real.
   Conferir: campo presente nos 27 envelopes e Σ `nv` == 513.
3. **Piso de 20% do QE: "candidato" ou "candidato não eleito"?** Adotada a leitura operacional
   (não eleito). Travar com fixture do simulado 2.

---

## Armadilhas desta base — leia antes de codar

1. **Relatório de subagent é hipótese.** Nas sessões de 11–13/09, **onze** erraram de formas
   plausíveis. Confira no disco, inclusive o que este handoff afirma.
2. **Teste que passa não prova nada.** Verifique por **mutação**, você mesmo. Três formas
   distintas de teste decorativo já apareceram aqui.
3. **Default silencioso em conversor de enum** já mordeu **3 vezes**. Procure ativamente por
   ternário, `??` e `default` perto de cargo, turno e granularidade.
4. **A prosa fica para trás quando o dado muda.** Contramedida: asserção **negativa** (o teste
   proíbe a frase errada) e valores derivados do payload, nunca literais no JSX.
5. **`Map.set` sobre linhas de par** é o modo de falha padrão desde a migration 0006. Toda leitura
   por `(uf, cod_zona)` precisa de `SUM(...) GROUP BY`.
6. **Não rode `constitution-guard` em paralelo com agente de implementação** — ele faz `git stash`.
7. **`*/5` dentro de comentário JSDoc fecha o bloco** e o formatador destrói o arquivo.
8. **Derrube o que subir.**
9. **Podem existir outras sessões do Claude na mesma pasta.** Em 12/09 havia uma.
10. **Sites oficiais bloqueiam automação**: `tse.jus.br`, `cdn.tse.jus.br`, `ibge.gov.br` e
    `planalto.gov.br` respondem 403 a cliente não-navegador. Por isso os PDFs estão versionados em
    `tse_docs/`.

---

## Comandos

```bash
set -a; . ./.env.local; set +a            # obrigatório antes de tudo que toque banco ou Vercel
pnpm typecheck && pnpm lint && pnpm test
.venv-model/bin/python3.14 -m pytest -q
pnpm verify-fatia-premise --fixtures tests/fixtures/tse/2026-sim   # Passo 0 do dia 15
pnpm tse:watch --once                     # exit 0 = sem mudança, 2 = mudou
pnpm edge-config:smoke                    # gravação real no Global Config
.venv-model/bin/python3.14 scripts/bench-deputado.py
```

### Push

O `gh` tem duas contas; a ativa é `cneeducacao` e o repositório é `sudomenna/salacofre`.

```bash
gh auth switch --user sudomenna && git push origin main && gh auth switch --user cneeducacao
```

**Devolva a conta** — há outro projeto na máquina que depende dela. E **o push agora dispara build
automático**: confirme com `source: git` em `/v6/deployments`, não pelo painel.
