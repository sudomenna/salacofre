---
title: Handoff — 2026-09-17, a spec 020 nasce e descobre que os testes escreviam em produção
description: Estado, pendências e armadilhas medidas para retomar em sessão nova
status: stable
date: 2026-09-17
branch: main
supersedes: handoff-2026-09-13.md
---

# Handoff — 2026-09-17

**14 commits, todos empurrados e no ar** (`575427e` → `90aa7a0`). Árvore limpa, `main` sincronizada
com `origin/main`, **um único branch** local e remoto (os outros 8 foram apagados; ver § Limpeza).

O trabalho começou com um pedido de produto — um gráfico da evolução da apuração — e terminou
encontrando duas coisas que já estavam erradas havia semanas: dois commits de conserto presos num
worktree desde 13/09, e a suíte de testes escrevendo no banco de produção.

---

## 1. O que entrou

### Spec 020 — Evolução da apuração

Gráfico de linhas: horário do boletim no eixo x, fatia de votos no y, **4 candidaturas**, em **duas
bases** (apurado e projetado). Decisões do dono em § 3.

| Fase | Estado | O que existe hoje |
|---|---|---|
| **0 — a tela** | ✅ | `components/atoms/charts/SerieApuracaoChart.tsx` nas 4 rotas e **nos dois ramos de cada uma**; `scale.ts`; `lib/utils/rank-parcial.ts` |
| **1 — o dado** | ✅ | migration `0009` **aplicada em produção**; `pct_atual`/`votos_atuais`/`dado_ts` gravados a cada ciclo; `EdgeSeriePorCandidato`; `seriePorCandidatoFrom`; motivo `sem_serie` |
| **2 — a série na tela** | ⬜ | **é por aqui que se retoma** |
| **3 — simulação** | ⬜ | fixtures de governador e senador |
| **4 — verdade** | ⬜ | replay 2022, e2e de perf e a11y |

Documentos: [spec 020](../specs/020-evolucao-da-apuracao/spec.md),
[design](../specs/020-evolucao-da-apuracao/design.md),
[ADR-0046](../architecture/adrs/0046-serie-por-candidato-limitada-por-construcao.md) (que **emenda**
o ADR-0032, com link nos dois sentidos).

### Dois consertos resgatados de um worktree

Estavam commitados desde **13/09** numa cópia de trabalho paralela e **nunca chegaram à `main`**:

- o conserto do lint (a `main` tinha 4 erros contínuos, e com o portão vermelho ele parava de
  distinguir regressão nova de sujeira velha);
- a esteira de CI com banco e Python, **e o hook de pre-commit**.

O `package.json` deles traz `"prepare": "git config core.hooksPath .githooks"` — e é isto que
explica um mistério: o `core.hooksPath` **já apontava** para `.githooks` no repositório (config é
compartilhado entre worktrees), mas a pasta só existia naquele branch. Na `main`, o git procurava
as travas num lugar vazio e não reclamava. **Trava configurada e desligada no mesmo gesto.**

Hoje o hook está ativo e barrou commits **três vezes** — duas por formatação minha, uma por um
arquivo de fixture de 3,1 MB que reprovava o lint inteiro (resolvido no `biome.json`).

### A suíte escrevia no banco de produção

Descoberto ao responder o dono sobre o que `projections` guarda. Havia **1.877 linhas de harness**:
1.233 sob cargos que não existem (91, 92, 93) e **644 sob o cargo REAL 1**, com as candidaturas
sintéticas 101 e 102 ao lado das de verdade (os candidatos reais têm id < 100).

Duas causas, as duas fechadas:

1. **A guarda era a ausência de `DATABASE_URL`** — e ausência é um estado que se perde por acidente,
   exatamente quando alguém carrega o env para outra coisa. Agora
   [`tests/integration/_guarda-banco.ts`](../../tests/integration/_guarda-banco.ts) é ponto único e
   exige `ALLOW_DB_WRITE_TESTS=1` **declarado**.
2. **O cleanup tinha furo de SQL**, e é ele que explica meses de acúmulo: filtrava `uf IN (…)`, e
   **`IN` nunca casa com `NULL`**. As linhas de escopo NACIONAL — justamente as que o modelo cria em
   todo ciclo — nunca eram apagadas. O teste limpava o que via e deixava o que não via.

Prova de que fechou: suíte completa **com** `.env.local` e **sem** a autorização →
`projections` 13.258 → 13.258, `snapshots` 921 → 921, 21 testes pulados.

---

## 2. Onde retomar — a Fase 2, em ordem

Tudo o que ela precisa já existe; o que falta é **leitura** e **emissão**.

1. **`fetch_series_por_candidato`** em `api/model/project.py` — irmã de `fetch_series_temporais`,
   com o downsample **dentro do SQL** (`DISTINCT ON` por balde de epoch, `ORDER BY dado_ts DESC` =
   último-do-balde, **nunca a média**). O SQL está escrito no [design § 2.2(d)](../specs/020-evolucao-da-apuracao/design.md).
2. **`anexar_ponto_corrente`** — a série é lida do banco **antes** de o ciclo ser escrito nele, então
   sai um ciclo atrasada em relação ao placar ao lado. Anexe o ponto do ciclo a partir dos valores em
   memória; **não** mova a leitura para depois do INSERT (custa uma segunda varredura no orçamento de
   60 s). Deve **chamar** `pct_atual_nacional_por_candidato`, não recomputar.
3. **Emitir** `por_candidato` em `build_uf_payloads` e `serie_por_candidato` em `build_edge_payload`.
4. **Ligar as props reais** nas 4 rotas (hoje passam `eixo={[]}` e `candidatos={[]}`).
5. **Testes T1–T7** do [design § 9](../specs/020-evolucao-da-apuracao/design.md) — e **aplique as
   mutações você mesmo**; neste repositório teste verde não prova nada por si.

### O que a Fase 2 herda (medido, não suposto)

- `pct_atual` nacional é **`None`, não `0`**, quando não há voto medido — inclusive para candidatura
  que existe no nacional e não aparece em UF nenhuma. Os furos são reais; **não os preencha na
  serialização**.
- O `COALESCE(dado_ts, ts)` do SQL é **carga, não defesa**: toda linha anterior à 0009 tem `dado_ts`
  NULL, e ciclos sem hora legível continuam gravando NULL (ADR-0038 D1 proíbe cair para outro relógio).
- O índice `ix_proj_serie (cargo, turno, uf, candidato_id, ts)` já casa com o `WHERE`/`ORDER BY` da
  consulta — o `DISTINCT ON` sai dele sem sort adicional.
- **O elenco das 4 é decidido no produtor**, com o comparador de `lib/utils/rank-parcial.ts`
  (`pct_atual` desc → `pct_projetado` desc → `id` asc). ⚠️ **Não** use o rank que o Python já calcula
  em `project.py` para UF — aquele ordena só por `pct_projetado`, e a divergência só aparece quando
  apurado e projetado discordam de ordem, isto é, exatamente na noite.

---

## 3. Decisões do dono — não reabrir

| # | Decisão |
|---|---|
| D1 | As 4 são as que estão **à frente no momento**; elenco recalculado a cada ciclo |
| D2 | Eixo vertical = fatia de votos da candidatura |
| D3 | **Duas bases** com alternância — e ela usa o `ViewModeSwitch` que já existe, não um controle novo |
| D4 | Senador: 4 linhas, 2 primeiras destacadas (por **espessura**, nunca opacidade) |
| D5 | Só onde há **uma** corrida: `/governador` e `/senador` nacionais ficam de fora |
| D6 | **Escopo de persistência = cargos 1, 3 e 5.** Presidente com os **dois** escopos (nacional e por UF — é o de UF que alimenta as 27 telas). Deputado Federal **fora**: 7.791 candidaturas × 480 ciclos ≈ 3,7 milhões de linhas/noite, nove vezes todo o resto |
| D7 | **A proibição do caractere `%` em fase pré saiu do teste.** Argumento do dono: *"0% apurado é uma verdade antes da eleição"*. Conferido antes de remover: os três casos que a motivavam seguem cobertos por guardas que medem a **afirmação** ("apurado", "apuradas", "vence no 1º turno", o `/27`). Ver a emenda no RF-161 da [spec 019](../specs/019-fase-pre-eleicao/spec.md) |

**Consequência de D1, registrada e aceita:** a candidatura que cai do top-4 some do gráfico
**inclusive do seu próprio passado**, e a ultrapassagem que a derrubou fica invisível como evento.

---

## 4. Armadilhas medidas hoje — o custo de não saber delas

1. **`pnpm test:py` está quebrado.** Usa o `python` do PATH e falha com `ModuleNotFoundError:
   pydantic`. O que funciona: `.venv-model/bin/python3.14 -m pytest`.
2. **Rodar só as suítes que você editou esconde defeito.** Commitei o widget com "tudo verde" e a
   suíte **completa** mostrou **12 falhas minhas** em arquivos que eu nunca abri — um bloco novo numa
   página entra no HTML que **outras** specs varrem.
3. **A varredura do RF-161 lê o `innerHTML`, atributos inclusive.** O `%` que derrubava a guarda não
   estava no texto: estava em `style={{width:"100%"}}` e `transform:translate(-50%,-50%)`.
4. **Teste verde + deploy verde não provam que o bloco existe na tela.** O gráfico passou nos testes
   e **não existia em produção**: a home tem dois ramos e ele entrou só num deles. Só um
   `curl | grep` no site publicado pegou. **Depois de publicar, confira no site.**
5. **Agentes travam ao escrever arquivo grande** (aconteceu 4×). Travou? `git status` + `ls`
   **primeiro** — nos quatro casos havia trabalho aproveitável, e num deles o arquivo mais caro
   estava pronto mas com 4 erros de tipo. `pnpm typecheck` é o detector, não a leitura.
6. **Duas mutações sobreviveram à primeira rodada** e renderam testes melhores: sentinela *coerente*
   (775 de 1.000 = 77,5%) faz a aritmética re-inlineada dar o mesmo número — use sentinela
   **incoerente**; e `"DADO_TS" in sql` continua verdadeiro com a coluna fora da lista, pelo
   `%(dado_ts)s` do VALUES — compare as colunas **posição a posição** com os placeholders.

---

## 5. Gates e baseline (medidos hoje, com a árvore parada)

| | |
|---|---|
| Vitest | **2.606 passando** com `.env.local` · 2.554 sem env, **zero falhas** |
| Pytest | **501 passando** (`.venv-model/bin/python3.14 -m pytest`) |
| Lint | **limpo**, 433 arquivos |
| Typecheck | limpo |
| Banco | `projections` 13.258 linhas, **zero lixo de harness** |

A única falha restante é **de ambiente e pré-existente**: `ResultPanelAvatar.test.tsx` assume Blob
não configurado, e com `.env.local` carregado ele está. Passa sem env.

---

## 6. Pendências abertas

| # | O que | Urgência |
|---|---|---|
| 1 | **Fase 2** — a série chega à tela | é o próximo passo natural |
| 2 | **Fase 3** — fixtures de simulação de governador e senador. Hoje só existe a de presidente (3,1 MB), então o widget aparece "indisponível" em `/uf/SP/governador` e `/uf/SP/senador` sob `pnpm dev:sim`. Proposta: gerá-las **só com as séries**, sem o array de municípios | média |
| 3 | **Fase pré não é revisável fora da home.** `projection-current-pre.json` é importada só por `app/(pres)/page.tsx`; as demais rotas leem o reader, que em dev não tem chave. O caminho de produção **está certo e coberto por teste** nas 3 rotas de UF — falta poder VER sem o Edge Config | média |
| 4 | **`dado_ts` por UF.** Grava-se o do **ciclo**; existe `relogio_uf` (ADR-0038 D2) no mesmo call site. Uma UF cujo boletim travou às 21h aparece com a hora do ciclo, não com a dela. Contraria o contrato escrito da spec — decisão do dono | baixa, mas é decisão |
| 5 | **Janela do eixo**: começa no primeiro boletim ou às 17h fixas? Proposta na spec: primeiro boletim, porque janela fixa desenharia 45 min de linha reta fabricada | baixa |
| 7 | 🔴 **16 RFs estão invisíveis para o gate de cobertura.** `RF-012.1`, `RF-012.2` (spec 012) e `RF-153..RF-166` (spec 019) aparecem **só** no índice "RFs adicionados pelas specs" de `traceability.md`, que **não tem coluna Teste** — e o `rf-coverage-checker` lê a matriz principal. É o mesmo modo de falha que já aconteceu neste repositório. Não foram promovidos à matriz principal hoje porque isso exige verificar os testes de cada um no disco, e cobertura inventada é pior que cobertura zero | média — **bloqueia promover a 012 ou a 019 a `shipped`** |
| 6 | ⚠️ **A Fase 3 mexe em `lib/dev/simulacao.ts`** — o arquivo que derrubou a produção por 5h30 em 17/09 (`join(process.cwd(), …)` fez o Turbopack traçar 22.572 arquivos para dentro de cada função, e **o build passou verde** com o aviso só no log). Use `join(/*turbopackIgnore: true*/ process.cwd(), …)` e confira o log de build por `traced unintentionally` | quando chegar a hora |

---

## 7. Limpeza feita

Repositório passou de **9 branches locais para 1**, e de **4 worktrees para 0**. Os 7 branches
apagados estavam integralmente contidos na `main`; o oitavo (`fervent-hofstadter`) tinha os 2
commits resgatados, conferidos **arquivo por arquivo** antes de apagar. O branch remoto
`s07/simulado-ready-hero-1t` também foi removido (SHA guardado: `8bfb6f2`). Registro em
`/tmp/branches-apagados.txt` e no reflog (~90 dias).

---

## 8. Prompt de retomada

Para colar numa sessão nova, com contexto zerado:

```
Retomando o SalaCofre. Leia primeiro `docs/_meta/handoff-2026-09-17.md` — ele tem o
estado, as decisões do dono e as armadilhas medidas na sessão anterior.

Objetivo desta sessão: a **Fase 2 da spec 020** — fazer a série por candidatura
chegar à tela. As Fases 0 e 1 estão prontas e em produção; a migration 0009 já foi
aplicada no banco.

Comece lendo, nesta ordem:
  1. docs/_meta/handoff-2026-09-17.md  (§ 2 diz exatamente por onde começar)
  2. docs/specs/020-evolucao-da-apuracao/design.md  (§ 2.2 tem o SQL; § 9 os testes)
  3. api/model/project.py — `fetch_series_temporais`, `pct_atual_nacional_por_candidato`
     e `linhas_para_projections`, que são os vizinhos do que você vai escrever

Cinco coisas que não se descobrem lendo o código, e que custaram caro ontem:

  1. `pnpm test:py` está QUEBRADO (usa o python do PATH). Use
     `.venv-model/bin/python3.14 -m pytest`.
  2. Rode a suíte INTEIRA antes de commitar, não só as suítes que você editou —
     um bloco novo numa página entra no HTML que outras specs varrem, e foi assim
     que 12 falhas passaram despercebidas.
  3. Depois de publicar, confira no SITE (`curl | grep`), não no teste: já houve
     um bloco que passava nos testes e não existia em produção.
  4. Há hook de pre-commit ativo rodando `biome check` na árvore inteira. Ele
     aborta o commit. `--no-verify` exige ordem explícita do dono.
  5. Os testes de integração que escrevem no banco só rodam com
     `ALLOW_DB_WRITE_TESTS=1`. NÃO declare essa variável com o `.env.local` de
     produção carregado — foi assim que 1.877 linhas de harness entraram no banco
     real ontem.

Regra de teste deste repositório: teste verde não prova nada. Para cada teste que
escrever, aplique a mutação correspondente você mesmo e confirme que ele fica
vermelho — e restaure o arquivo depois (`diff` para conferir).

Fale comigo em linguagem comum: não sou engenheiro. Jargão só quando não houver
substituto, e explicado na mesma frase.
```
